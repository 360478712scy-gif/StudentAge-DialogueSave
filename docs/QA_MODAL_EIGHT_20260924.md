# 弹窗及存档八项调整（2026-09-24）

## 实现范围

1. ADV 确认、跳过/回溯确认、备注编辑框打开/关闭使用整屏32单位水平百叶；对照录屏2.48–2.68秒，旧帧原位逐条揭开。设置/日志/存档整页仍沿用原转场。确认动作在退出动画完成后执行，期间保留暂停与输入遮罩；卸载代次阻止迟到执行。
2. 删除普通/CG底部按钮上方的黑色说明文字，动作/声音/状态保留。
3. 备注输入先绑定文字再启用 TMP_InputField（否则 OnEnable 不创建 Caret），深色3单位光标；入场完成后聚焦。修复不止更换颜色。
4. 用户确认仅改原版默认说明：玩家命名、地点、2005春式日期，季节文字着色；按存档地点选背景，未知地点回退家。手工备注优先。ADV保留对白/选项摘要/手写备注。
5. 设置恢复独立“模组配置”：游戏/图像/音频/模组配置/对话框五页，总跨度1176，间距32，高64，各宽209.6，缓存预热同步五页。
6. CG新增可选图像/缩放状态，预览显示CG资源。原适配器完全拒绝CG保存，现捕获并恢复原生CG子视图、文字与缩放；恢复不再执行CG解锁。普通存档空CG节点兼容，旧头不增加null字段。
7. 原版“复制存档”改为独立“自动存档”开关；开只显示自动、关只显示手动，快速档不混入，和备注/删除选择互不排斥。完整底层排列仍保留全部档型，筛选不删文件或索引。空自动槽不创建手动档。
8. ADV 首开存档页关闭 SaveView 资源预载转圈标志；Original/原版界面入口恢复原值，真正保存/读取加载流程保留。

## 验证及边界

- 生产编译通过；独立存储104项（包括普通空CG与CG图像元数据冷扫描/往返）通过。
- 定向隔离脚本：`tests/RuntimeModalEightQA.cs`，运行 `tools/run_qa.py --modal-eight --local-font-preview`；初始化经96项路径重写及原生身份隔离。测试仅写qa。
- CG保存/读取/再次捕获、CG卡片、弹窗方向、自动/手动筛选、输入焦点及光标实际网格、首次入口加载标志：最终证据见 qa/modal-eight-20260924/final。
- 全视觉、五标签应用/取消、多分辨率、中文输入法、男女/MiniCG/连续CG及模组长剧情按用户要求记 docs/PENDING_TESTS.md，随后续任务合测。不能将单CG样例当作全剧情或正式游戏验收。
- 早期检查发现普通CG空节点被当对象取值，已修复并加独立回归；其余中断来自测试夹具（只改文本未更新原生分段、未打开保存页、重复测试使用已占用槽位），均未更改正式存档。
- 本机生产版本已安装，正式1349文件基线未变；最终卸载代次回调保护为编译确认，其他定向证据在final。安装状态和哈希仅以本批 install-receipt.json 为准；隔离QA程序集不得安装，未发布工坊/GitHub。
