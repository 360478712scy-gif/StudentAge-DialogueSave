# 存档页调整与专项验证（2026-09-24）

已安装本机生产版本，未启动正式游戏、未发布工坊或 GitHub。安装收据：`qa/archive-refine-20260924/install-receipt.json`。

## 本批实现

- 去掉返回标题画面；保留返回游戏画面的票券艺术字，限制在白色区域内，修正超框。
- 详细显示注释改为圆形勾选框；主角性别选择只显示对应男女存档，保留左详情与右侧每页 12 槽比例。
- 原版存档读取真实存档头内性别，使用对应男女主角 Q 版与主角家中背景；对话档继续显示记录背景和说话角色，缺失角色图回退主角。
- 四张完整背景分别为读取蓝、保存粉、快读绿、原版黄；头部颜色融入水彩画面、底部保持深蓝，移除顶端染色覆盖条。
- 筛选后的显示位置与真实档案位置分开解析，空位保存/复制不会覆盖隐藏的另一性别档案。对话档保留原有周目写入与跨周目编辑限制，不改变存储格式。

## 验证

生产构建与源码摘要一致；`prepare_qa.py` 验证 96 处路径重写及原生身份隔离后，运行 `run_qa.py --adv-only --archive-refine`，最终全部通过。证据在 `qa/archive-refine-20260924/final/`：

- 四种背景、每页 12 槽、圆形开关、票券文字范围、去除返回标题按钮；实际截图目视确认。
- 对话档及原版男女筛选；原版家中背景和男女主角图集路径、可见图片检查。
- 复制到筛选后空白位置、再删除复制档；被隐藏的女性原版存档逐字节不变。
- 对话档保存/读取、复制/移动交换/备注/删除与原版完整字节复制及编辑流程；关闭百叶窗完成。
- 正式数据基线 1338 项未变；安装前确认游戏无运行进程，备份旧版后原子替换生产 DLL，未安装 QA 重写 DLL。

测试驱动调试中的失败（捕获租约、首帧布局、图集对象身份）保存在本批证据子目录；最终判断使用 final，不将测试驱动失败记为产品故障。未重复此前性能套件；完整长周目、全部分辨率、Windows 原生及两设备同步仍见 PENDING_TESTS。

## 美术资源

由内置 imagegen 编辑既有存档背景，先生成蓝色，再以蓝色生成粉/绿/黄变体；均为 1672×941。路径 `assets/adv-skin/archive-background-{blue,pink,green,yellow}.png`。原始提示词、参考图与输出对应见 `assets/adv-skin/archive-backgrounds-provenance.json`；无运行时顶栏色块替代。
