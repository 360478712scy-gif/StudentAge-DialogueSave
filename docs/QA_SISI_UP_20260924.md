# 思思模组低落期演出检查（2026-09-24）

结果：当前 ADV + UP 1.0.22 在 CrossOver 隔离游戏中，作者的“葬礼”事件 1277067 与“独处／葬礼过去三天了”事件 1277069 均到达末尾并通过原生“确定”交互。分别观察182、132个作者节点。普通对白、表情与背景、葬礼CG、拥抱CG已目视截图。未改生产代码或作者文件。

## 范围和证据

- 模组3664928586（回响思念的山谷-陈思思 V1.5-毕业旅行）、兼容模组3673850497由原生加载器载入。UP实际实例版本、隔离DLL位置见 `qa/sisi-up-20260924/plugins.txt`。
- 使用已有合成测试存档，原生 `ShowEvent` 进入作者事件，保留正文、分支判断、效果和末尾选项；不是从新游戏自然养成至初二，不证明全部分支或全模组通关。角色称呼“雨”来自测试存档。
- UP原包复制并经过相同托管写隔离扫描；仅QA将其外置资源查找目录指向作者资源的CoW副本。没有修改UP演出补丁、作者CFG或正式模组开关。正式数据1338项基线一致，输入文件哈希一致。
- 葬礼与独处最终截图来自两个会话：葬礼完整通过后，发现自动脚本未确认奖励弹窗导致后续截图被遮挡；独处已单独完整补测，未使用被遮挡的截图作为通过证据。脚本现包含原生弹窗确认处理；该处理本身不算本轮独立通过项。
- `qa/sisi-up-20260924/` 保存最终截图、两段轨迹、检查结果、UP加载日志、输入哈希及 verified.json。本机正式游戏未启动，没有安装新生产DLL或发布。

## 实际发现

作者 `EC2BUnofficialPatch/Minigame/CustomMinigamecfg.json` 不是有效JSON（缺外层对象，且有尾逗号），UP启动时报解析错误并跳过。当前两段对白及CG正常播放；此问题另记，未擅改作者模组。

## 重跑注意

`tests/RuntimeSisiQA.cs`，入口 `tools/run_qa.py --adv-only --sisi-up`；`--sisi-after-funeral` 仅补测独处。先在qa/runtime复制UP DLL，设置隔离_mod与enabled_mods为3664928586/3673850497/3782171338/3804899191，复制前两个资源目录到author-workshop，关闭隔离UP自动更新，再执行prepare_qa.py。结束恢复原隔离模组列表并移走额外UP DLL，不让下一轮常规两插件QA混入UP。本轮已恢复，隔离证明已失效，下一次启动须重新prepare。
