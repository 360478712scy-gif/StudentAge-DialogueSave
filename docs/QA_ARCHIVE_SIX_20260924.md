# 2026-09-24 存档页六项修正

本机生产版本已备份安装；正式游戏未启动，未上传工坊/GitHub。收据：`qa/archive-six-20260924/install-receipt.json`。正式存档1338项基线一致。

## 实现

- TIME/DATE/COMMENT原本是色块压住旧标签，露出两层边缘。现移除旧标签像素、改用完整生成的磨砂深蓝白边标签，文字随主题配色；素材来源和提示词在 `assets/adv-skin/archive-provenance.json`，最终资源为 `archive-information-tag.png`。
- 右侧改用解包存档页白底按钮的普通/悬停/按下三态，补齐首尾页、十页、单页六键；按原作坐标内移，保留渐变蓝轨道和54px纸飞机。灰色外观来自此前误用日志控件素材；可点击性现以实际指针点击验证。
- 返回游戏440px票券艺术字在白色区域居中，中心位于票券内x280。
- 12槽图片及NO IMAGE均加1.5px主题色细边框。
- 新增默认开启的“保持编辑模式”：开时成功操作后保留工具，关时成功一次即取消；失败保留选择。复制/交换/备注/删除共用结果路径。左下提示为“设置数据编辑后是否保持编辑模式状态。连续编辑时请勾选。”
- ADV在原生SaveView打开、刷新及已打开视图补挂时统一接入；处理旧页面残留，成功创建后才阻止原生刷新。Original继续原生布局。新增导航、开关、轨道沿用原游戏音效。

## 已验证

`tools/build.py`通过；源码和素材均与生产构建清单哈希一致。`prepare_qa.py`证明96处路径重写及原生身份隔离。隔离运行：`tools/run_qa.py --adv-only --archive-six`。

证据目录：`qa/archive-six-20260924/final/`。专用成功标记：`NATIVE_ENTRIES_TOOLBAR_LABELS_NAV_AUDIO_BORDER_KEEP_EDIT_ALL_FOUR_ON_OFF_OK`，无失败标记。

- 绕过工具栏，直接从原生SaveView保存/读取入口打开：分类正确，单一ADV页面，关闭转场正常；工具栏入口通过。
- 六个导航按钮经过实际UI射线/指针点击，单页增减正确；新开关与导航的悬停/点击各恰好触发一次音效调用。
- 四主题边框、标签完整素材、票券居中及右侧内边距检查通过；四配色截图留存，保持编辑模式和帮助截图已目视。
- 开关ON/OFF各执行复制、交换、备注、删除，保留/取消工具行为全部通过；仅操作隔离档案，结束恢复默认ON。

本次验证的是隔离对话样例中的原生入口和工具栏，未重跑完整标题/全部剧情、所有分辨率；音效验证为事件与原始资源接线，不代替正式设备听感。原版编辑共用结果处理，此轮未重复其此前已过存储事务测试。其他待测边界仍见 `PENDING_TESTS.md`。

## 后续边框反馈

上述1.5px边框已被追加要求覆盖：左右/底部改1单位浅主题色，标题重叠区不绘边；整数坐标避免半像素差异。新构建已安装，收据 `qa/archive-border-20260924/install-receipt.json`。六项功能无改动，视觉细调未再开游戏，缩放目视列入待测。
