# 2026-09-26 0.2.2 旧版本回收与存读档提速（已本机安装、推送并发布工坊）

根因：覆盖只新增修订、从不删旧正文，用户本机188个对话档中179个是被取代的旧版本（324MB，另有同量Retained副本）；每次扫描读取+SHA256全部文件，保存扫两遍。修复：扫描按大小+时间复用；覆盖后回收被取代正文进有界回收站。隔离游戏用用户存档复制品（550文件/408MB）：首次扫描14.7s→0.64s，保存8～12s→0.3s，读档12.1/4.3s→0.42/0.31s，435个旧版本回收，原有110个应保留文件哈希不变。128存储/26配置/16历史检查通过。见tests/RuntimeStoragePerfQA.cs、tools/storage_perf_seed.py。用户真实存档副本空跑：179个旧版本回收，9个有效档哈希不变且全部可读；安装前完整备份在qa/user-save-backup-20260926。工坊提交k_EResultOK且下载哈希核对通过，收据qa/releases/0.2.2/release-state.json。

# 2026-09-25 0.2.1 存读档分流修复已发布

保存页保存/覆盖，加载页只加载、空槽不写；覆盖取消与不再确认、跨周目原子替换通过。112存储/26配置/16历史及隔离游戏真实按钮读写通过。本机订阅版已更新；Steam成功回执及实际下载全部文件SHA256验证通过。报告 docs/QA_SAVE_ROUTING_20260925.md，收据 qa/releases/0.2.1/release-state.json。本次仅发布内容，不处理旧宣传图余项。

# 2026-09-24 0.2.0发布部分完成

用户明确授权发布，插件版本0.2.0与纸飞机UI大修封面、介绍已获Steam成功回执且远端元数据核对。五张宣传图第1张成功，其余从第2张因Steam上传超时中止；订阅下载完整性尚未确认。进度及原远端备份见qa/releases/0.2.0/release-state.json。正式游戏未关闭，未替换运行中本机DLL；本批仅版本号变化后重新编译/包级校验，沿用此前功能验收。

# 2026-09-24 标题保存入口与重复切屏小修

已去掉ADV对标题保存按钮的强制显示；标题加载页保存标签灰色禁用、空槽不能写入；当前存档分类再次点击立即返回，不重置页码/编辑模式，也不播放转场。禁用标签悬停保持灰色。按用户要求仅做编译、快速本机安装，定点待测记录PENDING_TESTS；收据qa/title-tabs-20260924/install-receipt.json。用户要求检查初次选择，正式配置DialogueStyle临时设Ask（其余配置不改，有备份）。

# 2026-09-24 最终合批检查完成并安装

[最终报告](QA_FINAL_20260924.md)：五设置页、四配色/阴影/票券、12槽整页、原版手动/自动筛选及非家背景、中文多行备注、全屏/MiniCG逐字保存恢复、水平百叶和退出清理通过。独立104存储/26配置/16历史/11确认/121契约通过。补上回溯延迟回调卸载保护并定点复测。正常全屏CG未缩小；用户所见悬浮画面为专门触发的原生MiniCG测试。

生产DLL已安装，最终收据 `qa/final-batch-20260924/install-receipt.json`；正式1349项基线不变，未启动正式游戏、未发布。真实输入法/听感、全剧情/多模组、Windows原生及双设备继续待测。下面旧段为历史状态。

# 2026-09-24 存档大外框阴影

原资源阴影合在file__bg0背景中，用户确认后按原图复刻轻微下偏的软阴影；面板原裁图/布局保留。编译通过并本机安装，收据qa/archive-shadow-20260924/install-receipt.json。按要求不开游戏，视觉合批验收；参数与来源见archive-provenance.json及PENDING_TESTS。

# 2026-09-24 弹窗及存档八项调整

已实现并本机安装生产版本，收据 `qa/modal-eight-20260924/install-receipt.json`。恢复五设置标签，弹窗开关整屏横百叶，去工具栏黑字、真实备注光标、原版默认玩家名/地点/季节色与地点背景、CG保存预览及状态恢复、原版自动档独立筛选、ADV首开存档去转圈。原版默认说明范围已由用户确认；ADV对白/选项/手写备注保留。

编译/104存储检查通过；定向隔离CG保存/恢复/预览、光标网格及截图、弹窗方向、筛选/入口标志通过。修复普通空CG节点错误；正式1349文件基线未变。最终卸载代次保护为编译确认。未启动正式游戏，未发布；五标签及全部视觉/输入法等按用户要求随后续任务合测，见 docs/PENDING_TESTS.md。报告：docs/QA_MODAL_EIGHT_20260924.md。

# 2026-09-24 存档角色、滚轮和入口已安装

- 保留12槽，滚轮整页；姓名普通字重，角色Q版缺头像先找本人的完整Q版，选项摘要“选择项：…/…”。首页入口和原版分类“原版界面”通过隔离验证。
- 票券只拉伸中段，纸飞机等比。最后用户追加文字右移8单位已编译安装，按要求与后续两任务合测，不再开游戏。
- [记录](QA_ARCHIVE_CONTROLS_20260924.md)，收据 `qa/archive-controls-20260924/install-receipt.json`；当前1349正式数据安装前后不变，玩家此前新增档未回滚；未启动正式游戏、未发布。

# 2026-09-24 预览边框追加修正已安装

- 原1.5单位半像素饱和描边改为左右/底部1单位浅主题色，标题重叠区域不绘边；不再出现顶部异色色线。构建/素材哈希通过，目视缩放检查留待下轮，不重跑无变化事务测试。
- 收据 `qa/archive-border-20260924/install-receipt.json`；前六项功能隔离验证继续有效。未启动正式游戏、未发布。

# 2026-09-24 存档页六项修正已安装

- 三标签完整素材、六个白底导航键/右侧留边、白区票券文字、四主题预览细框、默认保持编辑模式、ADV原生入口接管已实现。
- 针对性隔离指针与音效调用、原生保存/读取入口、四编辑工具ON/OFF均通过；正式1338项基线未变。详见 [本批记录](QA_ARCHIVE_SIX_20260924.md)。
- 生产收据 `qa/archive-six-20260924/install-receipt.json`；未启动正式游戏、未发布。旧报告为历史。

# 2026-09-24 存档页第二轮细化已安装

- 左下动画提示条、宽返回票券、原作圆角底板、渐变轨道/大纸飞机、删除性别入口；顶部加减管理总页数，保存与读取共用，非空末页保留。
- 针对性画面/指针/编辑/存读检查通过；原版物理编号忽略隐藏名单导致再保存不可见的问题已修复，新增关闭重开编号回归通过。正式数据1338项未变。
- 收据 `qa/archive-polish-20260924/install-receipt.json`；详情见 [本批记录](QA_ARCHIVE_POLISH_20260924.md)。未启动正式游戏、未发布。

# 2026-09-24 存档页细化已安装

- 返回标题移除、票券文字修正、圆形注释开关与男女筛选、原版家中背景及对应 Q 版、四张生成背景均已实现并隔离验证。
- 存档编辑/读取回归通过，隐藏性别档案字节不变；正式数据 1338 项一致。证据与边界见 [本批记录](QA_ARCHIVE_REFINE_20260924.md)，安装收据 `qa/archive-refine-20260924/install-receipt.json`。未启动正式游戏、未发布。

# 2026-09-24 追加性能优化已安装

- 日志窗口/有界记录行复用、票券字稿分帧预热、姓名和选项逐帧分配削减；两千条测试日志同步首开286→69ms，重复翻页3.20→0.56ms。计时口径与各分项通过证据见 [性能记录](QA_PERF_20260924.md)。
- 已备份替换本机生产DLL：`6abd7aeb43bf2a909b2e13fe01cde253d9e7bb772afd949a40c9b30bc04e1005`；收据 qa/perf-20260924/install-receipt.json。日志回溯/CG/选项隔离通过，正式数据1338项未变；未正式启动、未发布。

# 2026-09-24 思思模组 + UP 剧情演出专项

- 葬礼182节点、葬礼后三天独处132节点在当前ADV+UP1.0.22隔离实机中播放到末尾；截图/边界/作者小游戏JSON错误见 [专项记录](QA_SISI_UP_20260924.md)。正式数据基线未变，未改生产代码或安装发布。

# 2026-09-24 合批主要门禁通过，本机已替换

- 结果与仍待正式游玩/跨设备的边界见 [合批报告](QA_REPORT_20260924.md)，待测清单已精简为当前有效状态。
- 修复漫画图文门控、原版设置草稿重置、存档纹理/图集路径和缺图回调重入、原版编辑映射盘拒绝；保存/恢复/旧档/确认队列/日志/CG/卸载已隔离验证。
- 正式数据1338项基线一致；已备份并原子替换工坊本地插件，生产SHA256：`69f2727a08673274d5e56eef3ecb6f18cf08c4da6207d77668eeecf59d752014`。收据 qa/full-batch-20260924/install-receipt.json；未启动正式游戏、未发布/推送。
- 下面均为历史过程，旧“待合批/未安装”不代表现状。

# 2026-09-24 原版设置对话界面行：源码构建，待合批

- 对话界面移到快进速度下方，复用原生行和左右箭头/音效；确认提交、取消撤销，移除旧底部按钮。首次启动校园蓝选择页不变。
- 生产/合批QA编译通过，未启动游戏、未安装或发布；间距/主菜单与游戏内布局及交互待合批。收据：qa/native-mode-row-handoff-20260924/handoff.json。

# 2026-09-24 首次风格选择页：源码构建，待合批

- 首选页改用现有校园蓝背景、圆角卡片，ADV旁新增推荐标记；保留原版/ADV真实预览、整卡选择、偏好持久化及关闭流程，非首次取消复用票券艺术字。
- 生产和合批QA编译通过；未启动游戏、未安装或发布。合批目视/首次启动待测已记PENDING_TESTS；收据：qa/mode-picker-handoff-20260924/handoff.json。

# 2026-09-24 设置执行确认页：源码构建，待合批

- 新增对话框页，三列14组真实执行确认、全部ON/OFF；与不再提示共用配置，草稿仅应用生效，取消不写，全部ON能恢复重启后尚未绑定的旧原生确认。
- 原设置已有四项；已异步询问四/五项偏好，暂按保持四项将模组配置并入游戏滚动区；原总宽1176/框宽270/间距32不变。切页/音效/票券复用。
- 确认正文缩短行宽并按实际字体均衡换行，避免孤字，页脚提示同步；快速保存补执行确认与去重/取消释放。
- 11项独立配置/文字检查通过，生产/合批QA编译通过。未安装、未启动游戏、未提交或发布；原存档/性能批次未验收项继续保留。最新收据：qa/confirmation-settings-handoff-20260924/handoff.json。

# 2026-09-24 存档页重做：源码构建，未安装

- ADV存档页改为原作布局4×3，四标签按用户指定粉/蓝/绿/黄；左侧预览TIME/DATE/COMMENT，背景+说话Q版，缺图回退主角。原版模式保留旧九格。
- 四个互斥工具复制/交换/备注/删除；无工具空槽保存、非空加载，共用可记忆确认。快存轮换12槽。原生文件操作使用完整body/独立备份与原子排列索引。
- 原作卡片边框/详情板/游标复用；内置imagegen生成校园背景和六组艺术字。沿用顶部白笔刷与底部票券字稿、悬停点击音、进出/标签切换百叶窗；预览代次校验防串图。
- 独立存储99项通过；后续事务成员验证加强后18项定点复测通过；生产和合批QA程序集编译通过。未启动游戏、未覆盖正式安装、未提交/推送/发布。视觉、最终速度、原生API全链路与云同步不作为通过项。
- 当前源码/正式安装哈希见 `qa/archive-handoff-20260924/handoff.json`。后续先查 `docs/PENDING_TESTS.md` 最新存档批次及下方旧批次未完成项；数据库查 `ui.archive`。

# 2026-09-24 交互批次：源码构建，待合批游戏验证

- 日志/样式选择/确认/ADV存档和暂停菜单退出共享设置百叶窗；设置Esc关闭补齐同款转场，持续屏蔽穿透输入。
- 快速加载确认不再因已有弹窗静默丢失；确认排队、完成/取消释放暂停。全部通用确认入口新增独立记忆选项（原版保留原框），勾选并确认才保存。
- 按住移出保持pressed图案和字体；简繁语言仅留四字标签；默认白闭锁固定、开锁后按鼠标区域下滑/浮上，锁无持久蓝色。
- 日志未变更内容复用、素材分帧预热、头像异步GPU采样/回退。实际冷/热打开延迟与帧耗时尚待测量。
- 已检查：生产构建通过、9项原生API契约检查通过、合批QA程序集编译通过。未启动游戏、未覆盖正式安装、未发布；不重复运行此前无改动的存储测试。
- 本批构建 SHA256：`a783cf27b0901b51529fbc5a528237b79144a0189b37bfbbcbd669676a1ffdc9`；当前读取的安装 SHA256：`6c887b239d77874dbd6190796259d3b62c3190580ae505b1bf8d95e83b652f44`。
- 接续只读 `docs/PENDING_TESTS.md` + `qa/interaction-handoff-20260924/handoff.json`；前批漫画字幕/存读档性能/最终转场帧/完整恢复未验收项仍保留。

> 当前规则与检索入口：[项目知识库](knowledge/README.md)。以下是按时间记录的实现/验证历史，旧视觉和权限记录不自动成为当前要求。

# 2026-09-24 · 代码性能修复批次（源码已构建，未安装；按用户要求合批测试）

- 当前工作：漫画预读/首帧后字幕及隐藏操作栏、拳击鼠标原生跳过、原版模式使用原生设置、存档真实字节元数据缓存/目录预读/备份目录检查合并、HistoryTrail v2封存块缓存与世界差量、重建期间合并资源卸载、设置预热/缓存及首渲染后转场计时。没有重画UI。完整接续只看 [待测清单](PENDING_TESTS.md) 和知识库 `performance.batch`。
- 独立检查通过：Storage81、配置严格比较26及快照、HistoryTrail16。隔离中间版已过真实存读、原版设置外观、四页复用、拳击两次鼠标/资格、旧档兼容和缺节点预检。**漫画字幕返回断言失败后又做修正，最新尚未重测；转场首次渲染门控也未验收。** 不能称全通过。
- 用户要求减少测试次数、低风险合并到后续五项任务，立即停止重复全套运行；新任务尚未给出。剩余项目按PENDING_TESTS分级，发布前完成必要存储/恢复门禁。文档检查不启动游戏。
- 稳定证据 `qa/performance-handoff-20260924`；里面保存两次部分隔离结果，计时波动，不能当正式性能承诺。Steam曾连接失败，后续登录后正式游戏进程出现，QA保护拦截；未杀正式游戏，未继续启动隔离玩家。不要使用后来被正式游戏写入的qa/runtime/player.log充当隔离证据。
- 最终源码构建SHA256：`fb8acf16555f7955d1b36055e2251b61ab12583a402a5caad08ebe317c57e477`。正式工坊目录生产DLL仍是上一轮CG版 `6c887b239d77874dbd6190796259d3b62c3190580ae505b1bf8d95e83b652f44`；本轮未替换、未提交/推送/上传。精确安装路径及哈希收据 `qa/performance-handoff-20260924/handoff.json`。

---

# 2026-09-23 · CG 紧凑粗体姓名、灰白分隔线、黑色自适应底板与文字效果（本机已替换）

- 最终用户纠正为准：CG 去掉姓名底框/水印；姓名恢复【姓名】，与普通对白统一粗体，CG 正文仍不加「」。姓名居中，灰白高光+深色细边+下投影的分隔线比姓名宽两个全角字；姓名框/线/正文各留4px，较前版收紧。字体和底部操作栏沿用当前版本，旁白不显示姓名或线。
- CG 最终恢复黑色。使用原生 CGView 的 mask_talk_cg/img_mask_bottom，保留逐像素alpha和UV；完整文本一/二/三行分别约原生板高50/75/100%，底边固定，打字过程中不跳高。三行保持原生屏幕矩形。首次读取后缓存，普通对白底板不变；退出CG完整恢复普通水印/底板/对白括号。
- 现有反编译 qa/NewTalkView.cs 的 DoText、OnClickNext、ShowCG 及 qa/native-adv/CGUI.cs 确认原生文本路径和底板来源。加载剧情标签清单见 native-dialogue-markup.txt：字号20/25/30/35/50/55/60/70、橙色color，另有普通尖括号文本。原生 TMP/DOText 继续处理作者格式，不重写剧情/历史。
- 最终构建与隔离 --cg 通过并目视截图：普通/CG的绝对及相对字号、红绿嵌套色、粗斜体、下划/删除线、alpha/重置、上下标、逐字/点击补全，姓名【】/粗体/紧凑分隔线、原生alpha逐像素一致、三行原矩形、一二行降低/打字高度稳定、操作栏及退出恢复均通过。证据 `qa/cg-black-adaptive-final-20260923-231737`。早期上下标检查误用pointSize，最终按实际scale/baseline验证；中途蓝色固定底板与纯黑分隔线方案均已被最终要求覆盖。不是全剧情或任意第三方标签保证，未做正式剧情/云端验收。
- 用户已确认退出正式游戏；最终备份 `qa/local-install-backups/20260923-231737`，本机Workshop 3804899191生产DLL已替换，SHA256 `6c887b239d77874dbd6190796259d3b62c3190580ae505b1bf8d95e83b652f44`。未安装QA程序集、未上传Steam/GitHub，重新打开游戏生效。
---

# 2026-09-23 · 设置笔刷标签、等距艺术字票券、原作确认框与中文阅读字体（本机已替换）

- 顶部去除模糊，保留原作胶囊边框／下厚沿和选中圆润箭头。普通黑字后加 imagegen 白笔刷，仅 idle 显示，悬停、按下、选中隐藏；按最新反馈上下保持 43px，左右适度加长。
- 底部提示条宽 770；三个票券按钮统一 294px、间距 40px，与提示条间隔至少 24px。恢复默认与返回对话补齐艺术字，全部票券同款粗重倾斜字稿，悬停仍按状态变色。已写入 AGENTS.md；用户明确要求长期记住，另添加 memory update note。
- 恢复默认只重置模组草稿：水印显示、ADV、音量80、仅已读、文字30、自动49；取消保留原草稿，应用后保存。已有玩家偏好不静默覆盖。
- 说话人显示【姓名】，对白通过 TMP 预处理器显示「台词」，不改 native text/history。白字加细黑描边和右下黑影。旁白不强加对白括号。
- 确认框复用原作白底、按钮和圆形确认框，黄色头部换成 imagegen 校园蓝纹理；勾选／悬停为蓝色。覆盖本模组跳过／回退、恢复默认、ADV 保存覆盖／加载／删除确认，原版模式仍走原生确认。hover/click 使用核对哈希的原作 カーソル１／決定１，中央 AdvButton 统一触发，禁用项仍不点击。
- 汉化补丁 deffontmap.tjs 实际映射微软雅黑，但包内没有其完整文件；不混用日文字库、不打包苹方／微软雅黑文件。运行时一次读取安装的 msyh.ttc/ttf，校验 family；CrossOver 用严格限定私有 Font handle 的 TextCore 路由，缺失时回退游戏 Source Han Sans CN。最终 QA 确认实际为 Microsoft YaHei，已关闭之前默认启用的 QA-only 苹方替换（仅 --local-font 显式试用）。
- 最终构建、diff检查和 --settings-visual 隔离检查通过：四页缓存／百叶窗、笔刷四态、原作三态、等距与帮助框不重叠、所有票券 Image 字稿、恢复取消／应用、实际字体、括号不改原文、阴影材质、复选框与原作 hover/click 单次触发、日志返回。截图已目视检查，证据 `qa/settings-confirmation-final-20260923-211426`。另 `qa/choice-font-regression-20260923-210254` 的 --choices 回归通过，灰色禁用／悬停条件和原生分支一次执行不变。未做全剧情／云端／其他电脑无字体环境验收；计时只覆盖预热同步开页，非端到端帧率保证。
- 本机 Workshop 3804899191 生产 DLL 已原子替换，SHA256 `2c460f575185d1bb7a5f9654d83afffc113ceafea3ba41f6bb4b1b2dd8634344`，备份 `qa/local-install-backups/20260923-211426`。未上传 Steam/GitHub，未启动正式游戏验收，重启生效。

---

# 2026-09-23 · 黑色单边框条件提示（本机已替换）

- 最新用户纠正：条件提示不复用设置的白底信息贴纸，不采用选项框的双边框。用内置 imagegen 重绘黑色微蓝底、圆角、单层细边框和低对比纸纹；素材 assets/adv-skin/choice-condition.png，提示词与工具记录 choice-condition-provenance.json。生成器画出的棋盘背景已弃用，最终纯品红底资源在首次加载时抠色并缓存，游戏截图无底色残留。
- AdvChoiceTooltipSkin 只在 ADV 选项悬停期间临时装饰原生 DescriptionView，离开时恢复原生底图、字体、材质、颜色、留白与父级。仍使用原生条件回调、富文本内容、布局与悬停生命周期，不重新判断资格。默认文字为浅色，原生红绿状态文字只提高字形亮度以适应黑底；不可选项保持灰色、不可点击，仍能显示全部条件。
- 构建、git diff --check 和隔离 --choices 定点检查通过：单行／多行条件完整、富文本不改写、禁用选项灰色且阻止回调、可用性刷新、悬停与退出、共享原生提示层级与样式还原、可用选项原生分支一次执行及对白恢复。最终截图已目视核对。证据 `qa/choice-condition-dark-20260923-195424`。本轮未跑全剧情、存档或云端回归。
- 本机 Workshop 3804899191 生产 DLL 已原子替换并核对 SHA256 `722cc6c8d5d47f53ace22bd0a49713be5db10b95f443192f65ab4599b1a72e58`；备份 `qa/local-install-backups/20260923-195424`。未上传 Steam/GitHub，未启动正式游戏验收，重启生效。

---

# 2026-09-23 · 校园蓝色 Back Log（本机已替换）

- 日志按照原作 backlog_jp 的布局改为 1660px 行宽、约 200px 行高、正文左边界 x624、细横向分隔线、右侧到顶／上一页／下一页／到底和连续蓝色滚动轨道。复用解包圆形操作图标，保留历史人物／服装预览、完整检查点与原生回溯，底部只保留返回对话。
- imagegen 新生成蓝色纸张框架及日历纸飞机日期牌。用户明确保留第一版较大的 Back Log 标题；缩标题版本误把透明区烘成 RGB 黑色，已丢弃，恢复第一版真正 RGBA 透明图。最终游戏截图确认背景透出，新增中部 alpha=0 断言。正文遮罩采用原图 alpha230/255，色调 (8,39,72)；DATE 从当前 RoundCfg.year / SeasonCfg.name 读取，例如隔离画面的 DATE 2001-夏，不使用电脑日期。
- 打开日志使用设置同款 0.30 秒渐变蓝条百叶窗。GPU 截图只在打开时取一次，转场图层和输入遮挡高于日志；关闭或销毁日志清理未完成转场。素材首次初始化解码缓存，滚动仍仅实例化可见行。修正纵向 Slider 把纸飞机滑块拉宽偏移的问题。
- 最终构建和隔离定点检查通过：透明底、日期、标题、百叶窗前后画面、圆形滑块、连续定位、上下翻页与首尾、历史肖像、返回释放暂停、真实 EventSystem 点击回溯和确认后恢复完整手动对白。证据 `qa/backlog-campus-20260923-192117`，标记 backlog-visual-success.txt。此前检查分别暴露滑块比例／QA 固定条目索引与生成图黑底问题；最终结果不沿用早期通过记录。未跑完整剧情／存储／云端回归，计时仅为已预载素材后的同步构建耗时，非端到端帧率承诺。
- 本机 Workshop 3804899191 DLL 已替换，哈希 `27ee46a81559dd24abdb56056586d026608bc1146a551e3db1f76255987dafed`；备份 `qa/local-install-backups/20260923-192117`。未上传 Steam/GitHub、未启动正式游戏验证，重启后生效。

---

# 2026-09-23 · 原作选项框与校园剪影（本机已替换）

- 选项改用解包 select_off/over/on 底图，保留抗锯齿圆角、双边框和纹理；镜像未含拨片的右端圆帽替代左端，中央排布不拉扁圆角。原橙色悬停内框／按下底色只重映射为蓝色，禁用底图去色为灰色，文字保留作者富文本颜色。
- 用 imagegen 生成铅笔／尺子图集，作为两侧低对比剪影。常规两选项尺寸 1012×80，相比原图 1264×100 约缩小 20%；文字居中，宽文字可换行。素材／来源／提示词在 assets/adv-skin/choice-*。
- 选项出现时隐藏对白、姓名和完整对话底板；底部操作栏保留，在底部 90px 复用原作 window alpha 渐变并着淡紫色。隐藏／恢复选项不重新显示对白；选择后恢复原对话呈现。CG、漫画和设置边框规则未改。
- 构建与一次隔离定点检查通过：普通／蓝色悬停／蓝色按下截图，灰色禁用、原生条件浮窗及层级还原，资格刷新、富文本颜色、选择回调和对白恢复。截图已目视核对。证据 `qa/choice-source-skin-20260923-185159`，专用成功标记 choice-visual-success.txt；未跑完整剧情、云端同步或性能基准。
- 本机 Workshop 3804899191 DLL 已原子替换并验证哈希 `ed140d2f8ffc4f349ce815e3e3582d9c5d15ee9440e4fdc7ad8872615ef11da3`；旧版备份 `qa/local-install-backups/20260923-185159`。未上传 Steam/GitHub，正式游戏未启动验证，重启后加载。

---

# 2026-09-23 · 保留顶部原作上下厚薄边框（本地已替换）

- 原因：模糊层作为按钮的子图形在原作边框之后绘制，覆盖下沿部分像素，使原本上细下厚的边框变平。将模糊内部放到按钮后面的相邻层，原作三态边框纹理、尺寸与颜色保持不变；没有加粗或重新画边框。
- 编译、隔离设置定点检查通过；对比截图确认下沿厚度恢复。证据 `qa/settings-tab-rim-20260923-183651`，含 border-before-after.png（上旧下新）。本机 DLL 已替换，旧版备份 `qa/local-install-backups/20260923-183651`，新 SHA-256 `cb0d962da769e4c544f7230c1fa30cab6f9043ab0307760d7de47f9c0d364978`。未上传 Steam；正式游戏未启动验证。

---

# 2026-09-23 · 顶部模糊、百叶窗与悬停说明（本地已安装，未上传）

- 顶部使用原作 page0 三态胶囊：默认透明细边、悬停白底、选中深蓝圆润尖角。默认态内部显示按真实位置预先模糊的静态背景，圆角遮罩不覆盖外边；只在资源加载时计算。文字为 28px 普通粗体，默认深蓝及细米白描边、悬停深蓝、选中白色。中间按钮不改艺术字规则，底部保留艺术字。
- 顶部切页使用新参考视频中的横向 32px 百叶窗，配置区域和说明区域约 0.30 秒替换；顶部与底部操作按钮保持位置。开关设置继续使用斜向旧画面窄条和蓝色渐变细分隔线。临时快照仅在 GPU，结束释放；输入解锁跟随实际动画完成，修复独立计时器导致刚切完屏的点击偶发被丢弃。
- 新增 image_gen 重绘的蓝白纸张提示框，覆盖全部顶部／底部／中间按钮及滑块。按用户视频每 0.05 秒抽帧确认，淡入与淡出约 0.20 秒，无位移和缩放；快速移动由当前悬停对象决定说明，不阻挡点击。原始素材保存在 assets/adv-skin/settings-help.png，提示词见同目录 settings-help-prompt.md；现有导入器一次去除色键，徽标与圆角等比、只拉伸空白文字区域。
- 配置页和层级缓存复用；枚举值直接列出，删除标题列表图标及左右切换箭头。原生控制当前实例在回调时重新取得，保留取消回滚和应用持久化。
- 最新编译、隔离设置定点交互及实机截图检查通过；覆盖顶部点击百叶窗、模糊背景不挡点击、提示覆盖率与淡入淡出、连续悬停说明更新、滑块位置、单次音效、缓存重开、原生取消及插件应用。测量：warm_open_ms=6.5514; cached_21_tabs_ms=5.9686; built_pages=4。这些是同步回调耗时，非完整帧/首次启动性能保证。证据：`qa/settings-help-blur-blinds-20260923-182824`。
- 已按既有授权只替换本机 Workshop 3804899191 的插件 DLL，备份 `qa/local-install-backups/20260923-182824`，SHA-256 `672c3307364e51ef444beccc4444d0d09b782d200696345e4cd1b25ad3135d90`。没有启动正式游戏验证新 DLL、没有上传 Steam/GitHub；没有重跑完整剧情或云存档验证。

---

# 2026-09-23 · 原始配置卡与文字对齐（本地验证，未发布）

- 按最新要求直接使用提取的 option_01_jp 原始 frame 与 _jump_off 标题图标，保留圆角、浅色内边和纹理；配置卡间距 8px。标题图标、文字、滑条、右侧数值及短横线按原始布局坐标适配。
- 艺术字只保留顶部分类、底部操作及页面标题。中间选项改用普通文字；标题 22px、按钮 24px、数值 22px，采用源布局蓝灰色。字体仍使用游戏已有的 Source Han Sans CN，并非对参考截图字体家族的确认。
- 编译及一次隔离定点验证通过，目视检查模组页与音频页。连续滑动、滑块位置、按钮悬停／按下颜色及一次点击一次音效均通过。证据 `qa/settings-source-panels-20260923`；没有重新测试完整剧情、云存档或性能，也没有安装正式游戏或上传 Steam。

---

# 2026-09-23 · 设置页重绘与交互（本地验证，未发布）

- 设置布局采用顶部原生分类（游戏／图像／音频）及新增模组配置，两列配置卡与底部操作。实际显示／声音／语言设置继续调用原生控件及取消、确认流程；插件配置提供水印、UI 风格、按钮音量、文本速度、自动速度、跳过已读／全部及两个实时预览。
- 背景、控件、标题与按钮字形、卡片、滑条由 imagegen 重绘为独立贴图，嵌入并缓存。最终视觉以用户最新图二为准：顶部无下尖角、选中为无描边实色；卡片间隔约 12–14px，仅轻微边缘层次，无强蓝紫阴影与额外杂色矩形。滑条为一整根 8px 蓝色渐变圆头细条，纸飞机独立连续定位，不再分成底轨／填充两段。
- 常用按钮艺术字导入时提取单色字形覆盖率，去掉浅色外围装饰，居中显示；默认深蓝、悬停蓝、按下及选中白色，和按钮背景反馈同步。菜单初始化准备原生 UI 声道音效，按钮音量可预览，不逐次滑动播放。
- 已读跳过通过原生循环中实际到达的 TalkCfg 判断未读停止点，全部跳过把实际经过的节点加入原生已读记录，保留原本选项／小游戏／结束边界和效果执行。功能检查证据 `qa/settings-functional-20260923`：原生取消恢复、插件应用、预览、未读停止与跳过后已读均通过。
- 最终构建与定点隔离验证通过，已目视检查主页面、按钮悬停／按下截图。连续浮点滑动、纸飞机位置、艺术字颜色反馈、真实按钮点击及单次音效调用均通过；证据 `qa/settings-redraw-20260923`。早期截图暴露的字样左靠、杂色留边、压扁轨道已修正，旧成功标记不作为最终视觉验收。
- 未安装正式游戏、未更新 Steam 或 GitHub，未捆绑苹方。此轮没有扩大到完整剧情、跨机器或性能基准验证。

---

# 2026-09-23 · 纸飞机描线与散开循环（未发布）

- 替换原来的浮动/摆动：0.95 秒逐段描线 → 0.45 秒停留 → 0.6 秒线段分散、旋转淡出 → 0.18 秒间隔，总周期 2.18 秒。仍为透明内部的 30px 线框，固定在 (1644,1000)，动画只移动线段，不移动控件。
- 小控件使用独立嵌套 Canvas，30Hz 更新固定数量线段；不创建逐帧纹理、材质、协程或动画对象，不读取/重建台词网格。隐藏 UI 后停止，再显示从描线开始。
- 构建与一次定点隔离运行通过：描线/完整/散开截图、重复周期、锚点不变、隐藏停止、控件不阻挡工具栏点击。证据 `qa/plane-animation-20260923`；没有做完整性能基准或发布 Steam。

---

# 2026-09-23 · 苹方本地试排、空心指示器与蓝色按钮（未发布）

- 按用户最新要求，正文/角色名 36，CG 正文 38，白字浅黑细边；纸飞机改为 30px 纯线框、内部透明，固定在 1920×1080 坐标 (1644,1000)，保留轻浮动，不再跟随句末网格。
- 工具栏收起/展开改用原作 hold 锁的六个状态。常态白色、悬停亮蓝、按下及启用深蓝；载入时重着色，保留源图 alpha 和光晕。取代上一轮双箭头收起按钮。
- 原作确认/切换音已接入学生时代原生 UI 音频通道，不另设绕过游戏音量的播放器。
- 用户选择仅本机苹方试效果。隔离 QA 通过本地字体字节直达 FontEngine，确认 faceInfo 为 `PingFang SC / Regular`，避免 CrossOver 的 OS 字体替换；该路径仅在 QA 程序集，字体位于被忽略的 qa 目录，未嵌入 DLL、未安装到 Windows 系统、未放入 Steam 包。正式插件默认字体仍沿用原有回退。
- 编译与定点隔离检查通过：字体/字号、固定 30px 指示器、锁按钮悬停/按下/折叠位置、每次点击仅触发一次音效播放、隐藏恢复后的真实按钮命中、日志/跳过/保存入口。截图和记录 `qa/pingfang-preview-20260923`；CG 为模拟状态布局验证。未做全面剧情/性能回归、未部署或发布。

---

# 2026-09-23 · 本地 ADV 素材与 Logo 试排（未发布）

- 使用本地提取的 window 渐变 alpha 和 quickmenu 按钮图片，保留图集原始裁切偏移；普通对白采用源布局的文字区域，现有功能顺序保持不变。CG 保留更低、深色、居中的布局，漫画/过场规则未改。
- UI Logo 改为单行学生时代、小纸飞机、英文 Student Age；英文与中文交叠处由覆盖蒙版转为透明。底图和按钮来自提取资源，Logo 是新生成作品。所有图片嵌入 DLL 并缓存，不在逐字浮现中加载文件。
- 编译、33 张按钮状态尺寸/alpha 检查与隔离运行定点验证通过：普通对白、悬停、折叠位置、隐藏恢复后真实点击、跳过确认、日志入口、保存入口、未出现原版对话画面。CG 仅模拟呈现状态检验布局，未覆盖真实 CG 资源切换；没有重跑存储/云端或全面性能基准。证据：`qa/skin-preview-20260923`。新增验证脚本已修正 Canvas 恢复与异步开窗等待时机。
- 一次限定美观审查完成，无遮挡问题；快进与收起均为双箭头，依靠固定位置和现有悬停提示区分。未修改正式游戏程序集、未部署正式游戏、未上传 Steam。

---

# 2026-09-21 · 0.1.4 事件呈现与点击补全性能

- UI 来源独立记录 ShowEvent → ShowTalk → 选项延续。无事件对白使用原版 UI，不显示插件保存四按钮；事件对白遵守玩家的原版/ADV 设置。不能以复用 NewTalkView 上残留 evtId 判断。
- 原生 WaitBg、BlackBg、BlackBg2 在绘制前隐藏插件 UI；沿用 screenEffect 4008 的剧情 Mod 自动适用。只读扫描 14 个已安装模组中 148 处同类配置，其中《回响思念的山谷》3 处：1277126007、1277195005、1277198011。没有修改其配置；不代表所有剧情已逐一实玩。
- ADV 不再创建/维护隐藏的原版保存按钮，工具栏按已有对象状态核对；每帧只同步一次阅读呈现，句尾标记使用 TMP 已完成的网格，取消额外强制重建；早期稳定性判断先于 continuation 构造。
- 新增可选 presentationEvent 元数据，不改存档格式门槛；旧档缺字段时回退已保存 rootEvent。世界、选项效果、历史快照与原生点击状态机完整保留。
- 最终 0.1.4 构建集中功能回归通过：12 次逐字途中点击均只补全、不推进、不重复快照；事件/无事件切换、过场、旧字段缺省读取、条件悬停、自动/快进回溯、100 句世界效果回溯均通过。此前截图被夹具的异步旧 Mod 提示遮挡，修正 QA 后补做定点视觉验证，过场只显示原生“一段时间后”，闲聊为原版且无插件四按钮，事件 ADV 与句尾纸飞机正常。截图及记录见 qa/releases/0.1.4。
- 12 次同步点击中位 0.1516ms、最高 1.3532ms；点击后 24 帧 P95 18.0612ms、最高 23.2855ms（60fps 目标，完整帧包含引擎/渲染成本）。ADV Update P95 0.0881ms，不包括独立 LateUpdate；测试段原版工具栏重建 0 次。历史记录保留完整世界，不能把这些隔离数字当作所有机器/所有剧情零卡顿保证。工坊 0.1.4 已更新，Steam 返回 k_EResultOK；Windows 在线作者会话下载的 5 个文件 SHA-256 与本地发布包全部一致。CrossOver 会话被另一处登录替换，首次上传失败；改用同账号已在线的 Windows Steam 完成，没有重新登录或顶掉会话。上传临时任务已删除，隔离 QA 运行目录已恢复 Steam 写入拦截。

---

# 2026-09-21 · 0.1.3 选项状态修正

- 按最终澄清，不可选选项框体持续灰色，文字及原有颜色标签保持原样。
- 可选/不可选选项均沿用原生条件提示，已满足与未满足条件一并显示；仅悬停时出现在鼠标旁，移开隐藏。
- 修改仅涉及选项框体禁用色与撤销文字变灰；0.1.2 的恢复及富文本逻辑保持不变。
- 构建和隔离运行验证通过：框体持续灰色、文字不变、不可选选项的已满足/未满足条件、可选选项条件悬停、移开隐藏均通过。证据 qa/releases/0.1.3；工坊 0.1.3 上传返回 k_EResultOK；Steam 下载的 manifest 及全部内容与本地包逐字节一致。

---

# 2026-09-21 · 0.1.2 反馈修复

- ADV 选项启用原生 TMP 富文本，颜色标签不再直接显示；文字从原生正文控件读取。
- 日志回跳的独立恢复副本强制手动、正常速度；原始检查点不变，普通存档读取与只关闭日志的播放策略不变。
- ADV 选项绑定原生按钮可选状态，不重复计算条件；禁用状态灰字，保留可读底色。原生条件提示回调同步到 ADV 悬停区域，临时置于 ADV 上层；移开/关闭时恢复原生父级，不污染其他 UI。
- 隔离 CrossOver 集中回归 `DIALOGUE_FEEDBACK_FIXES_OK`，230 条运行断言通过。覆盖富文本字形颜色、原生分支点击、禁用点击拦截、条件提示、提示层级及复原、原生状态动态改变、三种自动/快进组合的日志回跳、取消/关闭保留原模式、对白/选项效果回溯及 100 个内存检查点。截图已目视核对灰字、粉色和悬停条件。
- 独立存储 67、配置 26、历史编码 10 项检查通过。证据在 `qa/releases/0.1.2/`。这些是隔离场景验证，不代表所有剧情模组已逐一验证。
- 工坊现有条目 3804899191：0.1.2 上传返回 k_EResultOK，订阅下载的 manifest 和全部 4 个内容文件与本地包逐字节一致；保留封面、介绍和四张截图。此次未再次启动正式游戏，功能验证来自隔离副本。

---

# 2026-09-20 · 0.1.1 Steam 测试包修复

本节覆盖下文旧的版本/插件严格匹配结论。模组更新不再作为旧档拒读原因。

- 恢复不再比较全局配置摘要作为门槛；对话元数据中的游戏 MVID、插件构建/版本及启用模组列表差异只记后台；原生返回流程仍以当前游戏适配契约核对。继续校验文件校验和、存档格式、所需对白/选项/角色/背景和返回上下文。实际节点缺失仍在废弃当前世界之前失败。
- 存档卡片的 Description 回调现在只在第 0 页返回台词，后续页返回 null；空槽复用时清除旧回调。原实现不区分页码，原生浮层因此反复追加同一句。
- 漫画图片按池单元请求代次接收回调，回收同步清图，不再排队加载异步占位图；已回收单元的旧回调不得覆盖当前图片。重开漫画时更新原生 id/page 参数。图片加载完成前不显示纯白空面板。
- 按最新要求，漫画演出使用原版文字、字体、位置和布局，入口同步撤下 ADV 层，不加对话底板和右下角四按钮；普通对白及真正 CG 维持原样。图片加载与池回收修复独立于呈现模式。漫画由多页图层逐步拼出三格，单独第一页只有左格是原生流程，不是丢图。
- 返回主菜单和正常关闭游戏共享一次退出自动存档，写入自动分类独立槽 2。正常菜单退出在 Game.ExitGame 前处理，窗口关闭由 wantsToQuit 接管；保存后继续退出，失败或最多 5 秒等待后也继续退出。修正退出回调重复触发和提前 Shutdown 取消保存的问题。
- Steam 包使用与小游戏插件一致的根目录 plugins/、preview.jpg、manifest.json、readme/。只包含本插件 DLL 和说明，不带游戏、BepInEx、UP、QA 驱动或存档。未调用工坊上传，也未安装正式游戏。

本轮验证与边界：独立测试 67 + 26 + 10 项通过。隔离验证已经实际读取测试环境遗留的 310107015 对话档，文件字节未改；故意改变版本、摘要、游戏构建和模组列表后仍可恢复，真正缺失节点在预检阶段拒绝。悬停分页终止、三次漫画开关、返回标题自动存档、注入磁盘异常后仍返回标题均已验证；正常 Application.Quit 后进程退出码 0，并在磁盘发现新自动档。漫画按真实节点顺序逐页呈现，截图 `hotfix-comic-2.png` 已目视完整三格、原版文字、无插件工具栏，四次呈现/重开检查通过。

限制：上述旧档通过不等于任意损坏文件都可恢复。CG/漫画播放中的完整存储、纸条/电话/未适配 continuation、强制结束进程与不可写磁盘仍不能保证保存；失败不得阻止退出。Steam 订阅后的真实加载和双设备云同步尚未实测，本包是待发布测试包。

新增回溯优化：逐句内存检查点不再遍历全局配置或读取插件 DLL，保留完整世界字节、随机状态、选项、效果阶段与返回上下文；相邻世界字节完全相同时共享不可变数组。跳转沿用实际读档的世界恢复路径，不重放中间剧情，不生成逐句文件或新增提示。最终效果语义和耗时见下一段。

最终合并专项：`DIALOGUE_HOTFIX_RUNTIME_OK`，260 条断言通过，正常退出码 0，退出后独立自动槽新增 revision `a46c96450b17404c82788b2dbc687795`。`history-rollback-effects.json` 证明选择 A 后跳回选项清除 A 效果、改选 B 只应用 B；跳回逐字阶段撤销后续对白和选项效果，继续后本句效果只执行一次。100 个逐字位置全部记录，逐句捕获中位 1.4565ms、P95 1.7243ms、最大 2.4466ms；两次跳转 450.5547ms / 424.1224ms。100 个无世界变化位置共享同一不可变世界缓冲，期间无新增磁盘存档。该数字来自本机隔离样本，不代表所有 Mod 和超大周目的上限。

打包 DLL 与最终隔离 QA DLL 比较全部 1058 个方法 IL，唯一差异是 `EnsureRepository` 内持久目录调用替换为隔离路径，业务代码一致。正式包保留正常游戏路径。漫画最终截图 `hotfix-comic-2.png` 已目视确认；未再次启动正式游戏。

正式数据只读核对：既有基线中观察到三个 Mod 文件及 player-registry 差异；三个文件修改时间均为前一日 23:16:20，早于本轮。保留差异报告，不覆盖基线或回滚用户文件。不能写成“所有正式文件与旧基线完全一致”。

---

# 当前状态

开发中，未交付完成版；本轮只更新项目构建和隔离 CrossOver 预览，未覆盖正式游戏。

## 2026-09-20 GitHub 仓库整理

- 仅整理仓库布局、构建工具、文档与独立测试，28个运行源码文件的内容未改变；没有启动或部署游戏。
- 纯数据测试从本机 qa 临时目录移至 tests，使用 NuGet 恢复 JSON 依赖，不再依赖另一组测试的 bin 输出。新增统一检查入口与 GitHub Actions。
- 本机构建成功，独立检查通过：存储67项、配置比较26项、历史编码10项。隔离重写工具可单独构建，但本轮未执行改写/启动。
- 首次源码上传排除 qa、research、dist、运行数据、个人路径、存档及依赖二进制；内嵌模式预览保留来源说明。此上传不构成正式安装包发布。

## 2026-09-20 箭头、逐字卡顿、删除重试与读档后历史

- 无字回转箭头改为连续圆弧/一致线宽，并加透明边缘抗锯齿；日志纸色从 242/240/234 微调至 230/228/222，alpha 仍为 0.86。已目视 `adv-real-dialogue-history.png`，真实历史人物服饰加载完成，箭头和分隔线清晰。
- 捕获开销主要来自逐句完整配置比较。比较器改成类型明确的表遍历、一次转换对象类型、直接字段/嵌套 List 循环、浮点位比较，并避免正整数键的反复字符串分配；比较范围和变更检测未缩减。相同一句从打字到完成/倒计时不再重复捕获，选项仍独立。
- 优化阶段实测：105句连续捕获由上轮约13.6s降至约4.2s，单次 `capture.total` 中位35.4ms、配置比较中位27.8ms。仍超过16ms，不能称零卡顿或已等同原版；实际逐字稳定帧与新节点捕获分开评估。
- 删除根因：页面 Run 在 service.Delete 前 Refresh；超过扫描间隔时 List 会设置 IsListing，旧 Delete 立即拒绝，导致第一次确认无效。改为排队等待扫描/写入结束，再原子删除指定 revision 并更新列表；重复 revision 合并回执。
- 读档后历史根因：原生日志文字随主存档恢复，旧插件的完整回看状态只在内存。新增可选压缩 `historyTrail`，与世界/对白放在同一个带完整性校验的 dsav 中；主线程在捕获时固定历史集合，后台压缩纯 DTO。读档前解包并检查周目、账户与日志前缀，恢复时关联当前已验证配置；每次跳转仍校验真实状态。没有另建需要云端配对的文件。
- 截图原生节点310107010、310107011、310107012均已实际捕获；保存成真正仓库档案，清空内存历史后再读取，4条恢复位置（含测试起始句）完整回来；三个原生节点已逐一实际恢复。此测试使用当前游戏真实对白/角色资源，但在隔离世界中运行，不等于完整新周目/全剧情验收。
- 新 codec 的10项数据往返/非法格式测试、配置比较26项及仓库67项通过。大规模历史仍受既有档案格式体积和解析防护约束，不能声称任意规模零成本。旧版本未记录的世界状态、普通存档文字、原生直接跳过未呈现节点、未适配特殊演出/回调仍不具备完整检查点。
- 后续用户反馈：人物预览改为对裁切内实际可见 alpha 范围居中，96×96 小样本只在首次资源加载时计算、同帧至多一个并缓存，空资源不显示白色块。恢复输入保护保留，但移除文字标签与额外暗幕。
- 完整专项最终结果见本节后续验证记录；先前失败留在 results-*：首次真实节点测试在上次恢复还未释放时调用 RefreshTalk，被原生保护阻止；随后用真实可推进条件等待。删除测试先错误使用超范围槽位/只 BeginMenu 未打开窗口；后改为实际工具栏打开保存窗口。恢复后同帧点按钮的测试需等待工具栏可交互，未放宽功能检查。

### 本轮最终验证

`qa/runtime/results/success.txt` 为 `DIALOGUE_ADV_FIRST_FRAME_AND_LOG_PREVIEW_OK`。截图中的三个真实节点捕获、磁盘保存后清空内存再读取、逐个跳转均通过；`history-disk-roundtrip.json` 保存四个恢复位置。`delete-during-scan.json` 证明扫描并发时一次请求获得一次完成回执，目标消失、原有存档全保留。234次绘制观察中原版图形、无效字体、“正在恢复”标签均为0。105句/106个位置保留与中段、最早位置实际恢复通过；最后一轮批量5.815s、capture中位38.8ms，早先同构建优化阶段批量4.2s，实际耗时有波动，不能用最佳值作承诺。

人物对齐最终图片 `adv-real-dialogue-history.png` 已目视；两张不同宽度头像现在位于同一列中心。首次小样本读取最慢12.342ms、共2次，后续缓存复用；这是首次图像成本，仍需注意低性能设备表现。隔离测试版保留运行并交还用户输入；正式游戏文件没有部署修改。当前仍未通过所有剧情/特殊演出、超长历史体积、真实重启两设备云同步验收；清空内存后仓库重读证明新档历史数据自包含，但不冒充完整跨设备测试。

## 2026-09-20 日志跳转确认与记录保留

- 删除日志左下角状态控件，包括等待、悬停缺失说明和错误文字；异常只写插件日志。
- 日志纸色由 250/248/242、alpha 0.90 微调为 242/240/234、alpha 0.86。
- 点击跳转先显示与跳过共用的 600×280 确认框；返回日志取消，跳转对话确认，复选框独立持久化 `ConfirmHistoryJump`。Esc 先取消确认并留在日志。
- 移除 96 条及 24MB 检查点淘汰；已读快进不再停止采集。原生 DoText、ShowOption、NextTalk 及推进前同步尝试记录，轮询只补充暂未稳定状态。逐字文本可按真实 Anim 状态捕获，无需等待全文出现。快照依旧在主线程同一时点读取，不把动画强制完成当作捕获。
- 选项新增独立日志条目与实际选项快照，不覆盖同一句正文。对白、选项实际跳转及两分支保留已通过隔离运行；确认取消、记住偏好、下一次不提示通过；首次实测确认后恢复约646ms，绘制检查无原版泄漏或字体丢失。
- **未达到“所有历史无条件跳转”**：旧版被淘汰/未捕获的状态、普通存档仅有文字的历史无法补造；现存特殊演出、未知 continuation 适配缺口仍在。缺少真实检查点的旧文字保留阅读，按钮仍不伪装可用。该阶段历史尚为内存状态；后续持久化修复见上节，旧文字仍不重建世界。不能将本次撤销数量配额等同于全剧情全状态支持。
- 最终专项通过：`history-retention.json` 共106个完整位置，105句快速连续原生 DoText 均在逐字阶段记录；实际恢复第50段和最早对白通过，不只验证按钮亮起。记录世界字节约5.72MiB，因此实际运行证明超过96条保留，24MiB淘汰代码已移除但未另造超过24MiB的真实世界测试。批量105句捕获耗时约13.6s，依然存在每句快照成本，不能称性能已无开销。
- 最新确认后的实际回退约650.9ms，394次绘制检查中原版图形泄漏0、字体无效0。截图 `adv-preview-history.png` / `adv-preview-jump-confirmation.png` 已目视检查。QA记住偏好测试结束后恢复 `ConfirmHistoryJump=true`，隔离预览停在日志。正式文件917个不变，唯一player-registry差异仍为旧基线已知差异。

## 2026-09-20 ADV、全屏日志及直接跳过

- 已新增可切换 ADV：淡暖白渐隐底、深色正文、CG 低位暗底及较大居中文字；姓名牌按性别/姓名长度变化，旁白隐藏；句尾黑边纸飞机轻微动画。正文继续由原生打字及效果状态机负责，按一次补全文字、再按推进。
- 模式选择页改为两张真实界面截图卡片，内嵌小尺寸 JPEG，关闭页面释放纹理。整张卡片可点击。说明按用户原文：“支持插件拓展功能（跳过,对话跳转等）”及“不支持其他拓展功能，仅支持对话中保存”。
- 工具栏右移，顺序 SAVE / LOAD / Q.SAVE / Q.LOAD / 日志 / 自动 / 已读快进 / 跳过 / 隐藏 / 设置。自动是纯三角，直接跳过独立三角加竖线，收起/展开按用户原要求保留左右箭头，旋转中心固定，避免位置漂移。
- 跳过确认框 600×280，保留后方对白/工具栏；无额外说明，两按钮分立左右；可记住“不再提示”。复用原生 OnClickSkip 直接遍历节点及效果，遇选项/小游戏停止；不是调高速度逐句播放。确认关闭后的暂停恢复必须完成才执行跳过，避免请求被原生过渡保护吞掉。
- 日志全屏、暖白半透明、无右上标语；人物左侧历史服饰预览，支持有现成图片的静态表情差分，Live2D 暂沿用原生半身预览。每句下方是无字回转箭头，无完整检查点时灰态并提供悬停说明；清晰的 2px 分隔线。滚动条两端到顶/到底，底部只有返回对话。
- 空格按下隐藏/恢复，原生松键事件同时被消费，避免一次按键切换两次；上滚打开日志，日志内滚轮只滚动，Esc 关闭。
- 原 UI 闪现包含真实恢复链路缺口：恢复绕过 OnOpen，先 ForceMeshUpdate 才接管，绘制探针实际抓到一帧原版。因此恢复在 ForceMeshUpdate 前同步调用 PreparingPresentation；同时保留已接管的新窗口，消除 Detach 后等下一 Tick 的空档。设置切回 ADV 也同步接管。完整 QA 中主动切回原版的对照独立保留，用户预览使用全程 ADV 专项。
- 日志 Esc 消费按下和松开两种事件，避免原生在松键时打开菜单。真实虚拟键盘按下、释放、等待后的原生顶部窗口断言通过。
- 紫色方块与慢重建来自 CloseAllView 回收资源包，日志却还引用字体。恢复期间通过原生引用计数暂时保留已加载的 UI 资源；新窗口接管后平衡释放，避免已被卸载的字体材质和反复读包。资源保护同时用于磁盘恢复和历史跳转。
- 内存历史已有完整独立配置副本，跳转直接严格比较实际字段与当前配置，省去重复生成磁盘格式摘要；磁盘档仍用原 SHA 校验。配置变动拒绝恢复的实际反向测试通过，拒绝发生在废弃当前对话之前。

### 隔离证据与性能边界

最终专项 `--adv-only --preview` 通过，`qa/runtime/results/success.txt = DIALOGUE_ADV_FIRST_FRAME_AND_LOG_PREVIEW_OK`。本次实际日志跳转 795.5ms；550 次绘制检查中原版图形泄漏 0、无效字体材质/图集 0。覆盖真实箭头点击后收起/展开中心不变、Esc 松开不打开原生菜单、配置变更在废弃当前状态前拒绝回退，以及新选择页两张图片加载与整卡点击。选择页和折叠截图分别为 `adv-preview-chooser.png`、`adv-preview-folded.png`。隔离游戏保留在新选择页，输入已交还用户。此项是合成对话场景验证，不代表所有剧情或磁盘存档的加载速度。

完整功能运行 `qa/runtime/results-125454/success.txt = DIALOGUE_ADV_FUNCTIONAL_OK_PERFORMANCE_SEPARATE`。实际输入验证空格、滚轮、Esc；点击真实检查点恢复；确认直接到选项且 timeScale=1；不再提示持久化；长列表箭头导航及人物图片加载。此完整运行早于最终无文字箭头与恢复布局空档修正，最终专项结果另外记录，不能混为同一个构建。

120 条日志面板创建在两次隔离运行约 8ms、14ms，仅建立可见附近条目。稳定帧 p95 原版 9.59ms、ADV 9.49ms，控制器 p95 0.174ms。**检查点捕获仍约首次 812ms、后续 105ms，未通过 16ms 门禁，不能称已经完全不卡。** 绘制流畅和存读档/历史快照耗时分别验收。完整 CG/漫画等存档恢复、小游戏全流程及跨电脑云端仍未覆盖。

仅一次美观/易用性子 agent 审查，5 张截图无遮挡/截断；建议收起图标与快进区分；曾改成上下符号，用户明确要求还原左右箭头，以用户要求为准。审查结束已停止，没有后端 agent 持续运行。最后无字箭头及分隔线由主 agent 目视核对。

## 2026-09-20 本轮纠正

- 保存成功留在存档页并更新卡片，可继续新建、覆盖；关闭窗口才恢复对白。
- 新建按钮一按即保存。原先只选择不提交；透明对白点击层还会挡住卡片。现通过原版 Tips 层打开对话中的整个存档窗口，保留上层原生确认框。
- 原版空槽 DisabledColor 会显示黄框；不再因整个页面 busy 把全部卡片设为 disabled。当前选中槽沿用原版选中样式，悬停只影响鼠标指向槽。
- 保存和读取均不调用右侧 Toast。成功安静刷新；真实错误留在本页底部，原页面在恢复过程中销毁后失败则显示原生错误框；自动保存失败记录日志与状态。
- 读档不再要求当前状态通过 CanCapture，也不再先保存当前世界作回滚。目标完整性/依赖预检通过后直接 RetireCurrent + CloseAllView，清旧回调与自动推进，避免结算已放弃剧情。只有执行恢复真的失败才清理至标题，不把返回标题当读档条件。
- 对话中原生手动、自动、快速存档三个页签可独立访问。切换时保留对话暂停租约，关闭或实际读入普通存档时释放。
- 本插件精确版本和存档 format/adapter 是兼容契约；自身 DLL 仅 UI/性能重编译不再使旧档因 SHA/MVID 不同拒读。第三方插件仍严格比较二进制，自身记录缺失、重复、版本不符仍拒读。

## 原版 UI 约束

工具栏实际宿主 TopView，新增四项在原 Tab/Shift/Esc 左侧；恢复不再额外打开标题 HotkeyView。S 与原生 UI/Navigate 冲突，实际 K/L/H/J。键帽沿用原版底图；本轮字体修正将原黑色细体改为原版common6烘字的棕色#734D31、偏粗无衬线和23–24/64键高的字面高度。原图没有K/L/H/J且无字体名元数据，因此字母采用匹配外观的动态绘字；中文标题固定克隆当前可见日志项的完整字体/材质样式。Esc 只增加原版保存/载入两项，不含快存快读或键帽，不改变原 group_btn 锚点、位置、缩放、尺寸和布局。

顶部固定“对话存档｜手动存档｜自动存档｜快速存档”；对话读档有自身手动/自动/快速二级分类，保存页没有二级分类。九宫格显示说话人与对白摘要。

本轮1920×1080独立目视审查及布局检查通过：单套工具栏、七项无重叠、原三项位置不变；Esc 与原生五项菜单参考位置一致；新截图无插件右侧气泡。真实输入设备悬停检查通过：目标 alpha=1 其余=0，移开全部=0。最初测试直接对第二槽发送 pointerEnter，未退出实际鼠标已悬停槽，制造了两个悬停；后续物理鼠标也可能覆盖模拟坐标。最终通过临时独立虚拟鼠标及UI输入设备过滤完成原生 Enter/Exit，结束恢复原设备配置，不改OS光标，门禁未放宽。

## 性能

原主线程每次全量配置序列化及哈希约3.7秒，总捕获约3.8秒。现主线程同一时点深复制世界和纯配置，后台用私有 serializer 与只读预检合同计算兼容摘要；不共享活动对象或 DataJson，不按 TTL 缓存配置值。配置类属性构造会访问 DescCtrl，因此类型计划只在配置和原生界面就绪后分帧预热；worker 禁止属性反射 fallback。

最新完整通过运行：窗口就绪约373ms；首次主线程捕获约647ms，后续约212–286ms；首次最大帧约654ms。仍有停顿，不宣称彻底不卡。实际全部游戏配置摘要与旧格式一致验证通过。类型计划预热采用软预算，个别首次表达式/JIT可超出单帧预算。这是上一轮基线。本轮已将读取配置哈希移到后台，并加入完整字段比较后复用摘要及一次标题空闲预计算；当前实测结果见下方本轮记录。

## 当前验证与证据

- 构建通过；纯配置副本兼容/深拷贝隔离检查通过；实际程序集配置类型检查通过。
- 隔离运行真实 K/L/H/J、原生鼠标点击、新建 .dsav 落盘完整性、保存后留页、悬停唯一高亮、覆盖取消、原三种读档类别、Esc入口通过。
- 原生普通存档实际读取通过，并验证旧对话暂停释放、不会复活被放弃的对白。
- 上一轮完整运行（见 results-* 归档）success.txt = DIALOGUE_CONTROLS_RUNTIME_OK。从不可捕获的未知回调选项场景，真实点击确认读取刚创建的对话档成功；保存的talkId、段落与周目一致，旧callback计数为0，暂停释放，原生普通LatestSaveKey保持不变。controls-05-actual-dialogue-restore.png是实际恢复后截图。
- 自身仅重编译兼容、第三方DLL改变拒读、自身版本不符/记录缺失/重复拒读共五个指纹校验均通过。完整运行留下隔离预览进程，未自动安装至正式版。
- 证据位于 qa/runtime/results 及保留的 results-* 历史目录。若存在 failed.txt，以实际失败点为准，不能把前置通过当整轮通过。

## 正式数据隔离

游戏托管数据和偏好调用重定向项目 qa/runtime/data，原生公司/产品名为 DlgSaveQA/DialogSave。测试只使用合成 fixture，未写原 AutoCloud 目录。本轮检查 918 项中，917 个正式文件内容不变；player-registry 相比原始基线已有分辨率与 Unity 会话计数差异（注册表时间为 2026-09-20 01:38:50，本轮启动前即存在）。LatestSaveKey 内容未变，未回写注册表或重设原始基线。旧915项基线及其他项目并发变更证据仍保留，未覆盖或回滚用户文件。

## 尚未验收

多分辨率与填满九格的视觉检查；所有对白/选项/限时/特殊CG/漫画/小游戏等恢复语义及效果恰好执行一次；未适配回调；UP/音频组合；全生命周期竞态；两设备 Steam 云同步。本轮真实控制流程通过也不能据此称整个插件支持所有状态。


## 2026-09-20 存读限制与损坏风险专项修正

- 本次“所有旧档插件不一致”实因隔离 QA 驱动每次重编译改变 SHA/MVID。仅 QA 程序在严格验证隔离路径、原生身份、重定向存档路径和恰好两个插件后，按其固定版本兼容重编译；生产插件没有增加第三方白名单，旧档字节不修改。
- 保存/读档忙时进入队列；重复请求合并。读档取消未提交的旧世界捕获，但等待已经开始的原子写入完成，避免取消回执掩盖实际提交。
- 独立提交回执不依赖 UI 调度队列存活。pending/完成/列表刷新逐观察者隔离异常，读档队尾在 finally 释放，后台禁止调用 UI。
- 本机跨进程锁把父版本校验与发布串行化；临时文件完整验证后原子发布。取消默认 2 GiB/4000 文件总量配额，旧历史不回收。
- 不再因 TalkCfg 含有后续小游戏入口而拒绝保存当前普通对白。实际小游戏进行中的状态仍需要独立适配。
- 正向 BGM 淡入可保存；已知纯音乐资源恢复失败/超时可降级静音，后台迟到的资源不会重新播出。未知含剧情回调的音频不能丢弃。
- 仓库 67 项测试通过，包括真实双进程争用只提交一份、父版本冲突、半文件、损坏、保留旧档和无默认历史配额。配置精确比较 26 项通过；相同引用、数量、TTL 均不作为一致性证据。
- 完整 controls 回归本轮止于旧脚本的“同页至少多个空槽”前提：历史增加后不成立，并非旧档加载结果。保留失败证据，使用 recovery 专项直接覆盖旧档加载和并发，不以放宽 UI 断言代替通过。
- 仍保留的数据保护：校验和、结构/版本、实际缺失或改变的剧情依赖、父版本冲突、权限/磁盘写入失败。删除这些保护不会补齐恢复能力，反而可能使残缺状态继续写入。
- 尚不支持完整保存的特殊状态包括 CG/漫画页、电话状态、纸条、正在进行的引导以及未适配 continuation。不得声称“全部状态无条件可存读”或“彻底排除存档损坏”。


### 本轮实际隔离验证结果

`qa/runtime/results/success.txt = DIALOGUE_RECOVERY_RUNTIME_OK`；QA 指纹自检 8 项通过。两轮实际恢复均读取上轮留下的完整旧档，分别验证节点、段落、世界周目、LatestSaveKey 和旧档 SHA。pending 和完成回调故意抛错之后仍可再次读取；保存请求中途读取也成功。不是重写旧档依赖信息后的假兼容。

第一轮重复读取 5870ms、快速保存 2222ms；加分段计时复核后重复读取 7236ms、快速保存 1334ms。第二轮恢复：配置比较约736ms、预检33ms、世界替换342ms、底层场景1453ms、对白窗口4432ms、音频4ms。剩余时间主要在窗口/资源重建，不能声称秒读或已完全解决卡顿。首次测试因标题预计算后注入合成配置，必须重新完整哈希，分别约15.4s/25.1s；其高冷启动开销没有被跳过检查或藏掉。

当前构建已放入隔离 CrossOver 预览并通过上述专项；未替换正式游戏插件。正式917个文件仍与原基线相同，原生偏好差异与本轮启动前相同。字体只由主 agent 快速查看恢复后截图；未再开 UI 子 agent、未展开新一轮 UI 验收。按用户要求已停止子 agent 工作。

## Steam 首次发布与真实订阅加载（2026-09-20）
- 工坊 3804899191，公开发布成功；0.1.1，指定封面与四张截图。
- Steam 返回创建、上传、订阅成功；下载每个文件 SHA256 匹配包清单。
- 隔离游戏原生 Mod manager 发现订阅，SaveModList 写入启用 ID，重启后原生 MultiFolderLoader 直接从真实 Steam 下载目录加载 DLL，初始化与独立宿主通过。没有向正式游戏插件目录复制 DLL。
- QA 初次缺少官方 MultiFolderLoader（prepare_qa 只复制 core）；补齐正式测试版原生 patcher 后复核通过。Z:/C: 同一 Wine 文件的路径别名比较改为路径尾部+字节一致性。
- 证据 qa/steam-publish/{status.log,download-integrity.txt,enable-checks.txt,verify-checks.txt,verified.txt,subscription-first-start.png}。这验证 CrossOver 原生订阅加载，不等同原生 Windows 全剧情或两机云同步验收。
- 留言开关尚待作者在浏览器登录后设置；不宣称已开启。

## 2026-09-21 待验证 0.1.5：过场反馈、卡顿与本地副本
- 实际运行证据：开始排查时 CrossOver 正式日志加载 0.1.3，本机订阅仍为 0.1.3；用户更新后 Player.log 已加载 0.1.4。0.1.4 原生 WaitBg 过场隐藏先前已有隔离画面证据，不据此保证所有 Mod 演出覆盖。
- 正式旧日志全量 config.compare 通常 200–280ms，最高 1126.8ms；一次 capture.total 332.5ms 中 plugin fingerprint 86.8ms、config compare 238.7ms，世界序列化仅 3.2ms。0.1.5 删除所有正常捕获路径的全局配置/DLL 指纹诊断及 Update 全量配置预热；真实世界/效果/随机/继续上下文、文件校验不减少。旧指纹诊断 helper 留给独立检查，不走运行时路径。
- 本地存档调查：CrossOver Saves/<SteamId> 找到75个版本（期间玩家持续游玩增加），日志已提交的5个 revision 全部仍存在；读取列表按槽位只展示最新 head，不能以版本数认定用户报告的具体几份已找回。已复制当前原文件到 qa/recovery-intake/2026-09-21，无修改源文件。等待用户提供丢失档类别/时间/台词定位；不归因于云错误或更新，尚无足够证据。
- 原设计只在显式删除时备份，缺少每次保存的独立副本。0.1.5 生产 Repository 开启 Retained 副本，后台补已有档/恢复缺失档，保留 tombstone 和冲突分支，不覆盖损坏或同名文件。备份失败不撤销成功的主提交，只记诊断；本地备份在 DialogueSaveLocal/<SteamId>/Backups/Retained，脱离云存档目录和插件安装目录。
- 新增离线回归覆盖副本逐字节一致、云文件消失恢复、删除标记不复活、坏源/坏副本保护、已有档迁移、备份不可写、双分支保留。扩展隔离 QA 代码覆盖真实自动保存期间整帧耗时、提交后读取及整段 WaitBg 每帧遮罩。
- 用户正在游玩且明确暂不方便退出，本轮仅改代码与离线检查，不启动游戏/替换在用文件。0.1.5 未实机运行验证、未发布 Steam；最新已发布仍为 0.1.4。不承诺已经消除所有卡顿或找回所指历史档。

### 0.1.5 用户恢复测试后的结果
- 最新授权取消事件分类：ADV 对普通对白统一启用，原生过场/漫画例外不变。
- 修复读档保护期内被跳过的 RefreshTalk 仍触发 ADV 隐藏的问题；尊重原生执行结果及恢复租约。首次测试在不符合原生时机的自动存档断言停止，修正测试后发现并修复此隐藏问题，最终集中回归通过。
- 隔离运行 DIALOGUE_FEEDBACK_FIXES_OK：自动档3次提交及读取、本地副本、无事件 ADV、逐帧过场无残留、旧元数据兼容、富文本选项及真实世界/选项效果回溯均通过。点击补全最高1.74ms，其后帧P95 17.46ms；自动保存同步调用最高10ms，但写入期间完整帧P95 49.15ms/最大64.53ms，不能宣称所有卡顿消除，合成世界56KB亦不代表所有真实大档。
- 76项独立存储检查、26项配置比较、历史编解码检查通过。证据 qa/releases/0.1.5。

- 0.1.5 已更新工坊3804899191，Steam返回成功、真实下载5文件SHA256匹配。本机CrossOver对应订阅包也已更新并保留旧包备份；未启动正式游戏，下一次启动加载。
