# 存档页第二轮细化（2026-09-24）

已验证并安装本机生产版，收据 `qa/archive-polish-20260924/install-receipt.json`；生产哈希 `d1bad7492ed7e44f6784a1c2bcbcce3d5ca8b2b28e1527c100c4d4a5fca83be2`。正式数据1338项未变，未启动正式游戏、未发布。旧版男女筛选入口已按用户最新要求移除。

## 最终行为

- 左下读取/保存提示、工具说明及操作结果共用校园风格提示条；取消框外小字。悬停淡入、离开淡出，无提示时不占视觉空间。
- 返回游戏票券由 322×70 加宽到 440×78，继续使用艺术字，左侧提示条与按钮不重叠。
- 纸飞机滑块为 54×54，对照原作 50×50；轨道复用之前生成的渐变蓝条，加入完整轨道点击区域。
- 中间使用解包 file__bg0 的原作灰白渐变圆角底板，保留原尺寸位置，取代纯色直角块；顶部和底部校园背景不变。
- 删除性别按钮和选择弹窗，显示现有男女存档，Q 版仍按存档性别识别。
- 顶部原作减号/加号分别移除最后一页空页、在最后追加一页；保存与读取共用配置并持久化。末页有档时拒绝减页，并在左下说明；至少保留一页。右侧箭头与轨道负责切换当前页。

## 证据和边界

- `qa/archive-polish-20260924/visual-pass/`：两种性别均可见、原版男女Q版/家中背景、四背景、提示条淡入淡出及布局、加减号真实命中和保存读取共用页数均通过。实机截图 `archive-refine-category-1-Male.png` 为本轮效果。
- 旧回归脚本假定第十页为空，但多次隔离测试已占满预留页；此断言失败后改用加号创建新空页，不删除已有测试档。通过的画面检查不重复。
- 对话存读与编辑、轨道和右侧灰色箭头点击已通过，见 `native-hidden-name-failure/checks.txt` 中对应断言；原版专项修复后的完整通过记录见 `edit-pass/`。正式数据、安装结果以本批收据为准。
- 原版专项发现并修复：关闭重开后物理编号分配忽略旧隐藏名单，重新保存同名档被隐藏。新编号同时避开删除保留名称，并在新档无法发现时报告排列异常而不是静默成功。失败现场保留在 `native-hidden-name-failure/`，仅重跑原版专项验证修复。
- 不改事务存储算法、原生游戏存档格式或快存轮换12槽规则；完整长周目、全部分辨率、Windows原生及云同步仍见 PENDING_TESTS。

## 素材来源

直接复用解包 `file__bg0.png` 中 (80,120,1760,840) 的面板，圆角处只清除外部黄色；加减按钮使用 `file.json/_pgbtn` 的三态坐标。轨道、滑块、提示框、票券艺术字复用现有生成素材，本轮没有另做生成图。完整对应见 `assets/adv-skin/archive-provenance.json`。
