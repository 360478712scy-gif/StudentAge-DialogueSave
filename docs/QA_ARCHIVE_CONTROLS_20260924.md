# 2026-09-24 存档滚轮、人物与入口

本机生产版本已备份替换；收据 `qa/archive-controls-20260924/install-receipt.json`。未启动正式游戏、未发布。

## 当前要求和实现

- 用户纠正“16槽”：最终仍是12槽4×3，预览220×124、原布局不变；滚轮任何幅度只切整页，0.16秒防抖，弹窗/备注/忙碌时不切页，不出现半槽。评论区域保留自己的文字滚动。
- 姓名和【】不再加粗，阴影及正文富文本保留。
- 说话角色Q版先用本人的头像图集，缺图尝试本人完整Q版，最后才用主角。原游戏 `role_comic_head` 不含 `role_qingya`，但 `role_comic` 有，因此肖清雅此前错误回退主角。旧档缺角色编号时也可用明确姓名匹配；原版档仍按存档性别显示主角。
- 选项从实际显示的选项单元读取，保存 `choiceSummary`，格式“选择项：xxxx/xxxx”。扫描优先保留选项摘要，不用正文覆盖。旧档记录的选项ID可生成兼容摘要；旧档没有存下当时显示文本，只能按当前配置解释。备注仍优先。
- 首页ADV保存入口允许浏览；空槽写入仍需可用游戏进度。原版分类的交换按钮改为“原版界面”，当前打开的视图绕过ADV接管，关闭后再次打开恢复ADV。
- 所有纸飞机票券采用横向分段拉伸：左图和右缺口按高度等比，仅拉伸中间；存档返回文字按白区布局。最后用户要求再右移一点，已追加8单位光学偏移，不变按钮或图案尺寸。

## 验证和边界

生产构建通过，源码/素材哈希与清单一致。隔离准备证明96处路径重写及原生身份隔离；`tools/run_qa.py --adv-only --archive-controls` 专用结果：`12_FULL_SLOTS_WHEEL_SPEAKER_CHOICES_NATIVE_ESCAPE_TITLE_ENTRY_OK`。证据在 `qa/archive-controls-20260924/final/`。

实际指针滚轮大/小幅度均只切一页；12完整槽、姓名非粗体、肖清雅本人的图集像素区域、选项保存摘要、原版页面可见及不会被重新接管、再次打开ADV、无对话上下文首页按钮均通过。票券分段参数和等比缩放断言通过，12槽人物截图与选项截图目视通过。独立存储检查101项通过。

测试脚本曾因同帧访问已销毁卡片、误用图集克隆对象身份比较、关闭窗口后立即推进而失败，均修正为等待生命周期/比较纹理区域/等待输入释放；对应历史证据保留。缺头像而完整Q版存在是真实资源分布，已修复回退链。

最后8单位文字偏移只编译，按用户要求暂不重开游戏；与后续两项任务合并目视验收。其余长周目、多模组、多分辨率和全设备听感仍未宣称全验收。旧原版UI交换按钮测试不再适用，原版交换存储层历史验证继续有效。

旧1338项正式数据基线与本轮核对不同：玩家在15:14–15:17已新增真实剧情存档和原版保存，早于本轮隔离测试；未回滚这些数据。另存当前1349项安装前快照，安装后全部一致。测试路径及原生身份隔离证明、真实存档时间/摘要已核对。
