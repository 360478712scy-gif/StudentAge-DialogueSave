# 2026-09-24 合批测试报告

本批主要功能、数据安全、恢复和卸载门禁通过，已备份并替换本机正式 DLL。测试使用独立游戏副本、独立存档路径和原生身份；未启动正式游戏做游玩验收，未提交/推送/发布工坊。

## 用户看到漫画点不动的原因

自动测试时 RuntimeAdvQA.IsolateInput 会临时屏蔽真实鼠标/键盘，避免手动点击改变测试步骤；此前没有明确区分自动测试窗口与可玩预览。同时漫画首图/字幕门控确有失败，已按图片完整 URL、代次和淡入状态修复；连续 1→2→3→重开 1 已通过。截图不能替代手动游玩验收。

## 本次修复

- 原版设置行的编辑草稿被周期刷新覆盖：同一次编辑只初始化一次，取消/确认各自保持语义。
- 漫画首图就绪后再恢复原生字幕；普通对白及操作栏继续隐藏，旧页回调不串入新页。
- 存档背景路径在 Windows 下误用反斜杠；Q版头像误走纹理加载；缺图回退在原生回调中重入导致集合迭代异常。分别修正路径、图集加载及下一帧回退。
- 存档滑轨手柄恢复圆形，底部艺术字调整到票券白色区域内。
- 原版存档编辑误拒绝 CrossOver 映射盘根：沿用 Repository 的卷根边界，卷根下链接目录/文件仍拒绝，并补两项回归。
- 测试脚本补齐新 UI 的首次布局、原生加载遮罩和退出动画等待；这些属于测试时序修正。未保留试验性的恢复遮罩强制关闭改动。

## 通过证据

| 检查 | 结果与证据（qa/full-batch-20260924 下） |
|---|---|
| 独立数据检查 | 存储99、严格配置26、历史16；确认11（confirmation-check.log）、元数据契约121（adapter-contract.log）。原版编辑新链接保护定点20（native-link-check.log）。 |
| 漫画/拳击/原版设置/性能 | hotfix-attempt5 的性能、原版行和拳击断言通过；hotfix-pass 最终连续漫画、旧回调、完整世界效果回溯、兼容、退出保存与注入写入失败通过。 |
| 12槽对话存档 | archive-attempt7 完成四分类12槽、实际保存/读取、备注替换、复制/空位交换/删除/工具取消，截图背景与Q版均正常。该运行后续原版测试失败，后续单独修复验证。 |
| 原版黄色页 | native-pass-interaction-attempt1 的原版创建、复制完整body、空位交换、删除、源字节不变与退出动画全部通过；此运行后续快读测试时序失败，后续验证见下一行。 |
| 快存/快读及首次选择 | interaction-pass-settings-attempt1 中 interaction-success.txt：13次快存轮换12槽、取消不记忆、勾选确认后持久化、排队快读、取消重试、实际恢复、按住移出颜色稳定、锁定切换。首次双预览及两模式持久化通过。 |
| 设置 | interaction-pass-settings-attempt1 完成原图按钮/四页/白笔刷状态/提示淡入淡出/票券间距/恢复默认/字体与音效检查；随后新确认页等待时序失败。settings-backlog-cg-pass 完成全部ON/OFF草稿/取消/单项覆盖/应用/读回配置及正文括号、阴影、声音。 |
| 日志/CG | settings-backlog-cg-pass：透明度/日期/肖像/虚拟行/滚动/复用/进出百叶窗/真实回溯；CG粗体括号姓名/紧凑分隔线/无水印/黑色底板1、2、3行/富文本效果/恢复普通对白全部通过。已目视相关截图。 |
| 恢复及异常 | recovery-pass：旧版本存档两次真实恢复、节点与世界正确、旧档哈希不变、回调异常不漏暂停、保存中读档成功、原生LatestSaveKey不变。 |
| 卸载 | lifecycle-pass：缓存日志、存档页和确认窗口存在时销毁宿主；无遗留插件画布/输入遮罩、自有补丁清空、原版存档子控件和对白字体恢复，日志无清理异常。 |
| 隔离保护 | live-baseline-verification.json：1338项正式数据、代码和偏好完全一致；测试期间正式工坊DLL未修改，安装另有明确收据。 |

各阶段独立通过可以合并为覆盖矩阵；含 failed.txt 的阶段仅采用失败之前的通过断言，不能把该进程整体标成成功。通用 success.txt 名称较旧，实际范围以各专用 *success.txt、checks.txt 和本表为准。

## 性能实测及边界

小型隔离样例：存档菜单就绪624ms，保存1006ms，读取543/525ms；设置准备246ms，转场运动帧约6–11ms。日志素材已预热时准备37.5ms。后续旧档恢复样例读取965/712ms、快存1861ms，说明不同历史和存档数量仍有差异，不能承诺所有剧情瞬时存读。100条历史回溯约126ms，捕获中位数2.46ms、P95 2.88ms。

尚未覆盖：正式完整长周目和真实多模组组合、Windows原生机器、两设备云同步/原版排列索引合并、全部分辨率及离焦/真实鼠标靠近底边的手动观感、全部剧情过场。当前首启预览图沿用此前内置截图；不把它当当前全部美术效果的实机证明。

## 安装

生产 SHA256：`69f2727a08673274d5e56eef3ecb6f18cf08c4da6207d77668eeecf59d752014`。旧 DLL 独立备份，安装文件与生产构建哈希一致。路径/时间/备份见 `qa/full-batch-20260924/install-receipt.json`；未安装 QA 重写程序集及测试插件。正式游戏未自动启动，下一次正常启动使用新版。
