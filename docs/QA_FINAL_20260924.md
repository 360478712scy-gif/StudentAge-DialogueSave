# 最终合批检查 · 2026-09-24

本轮检查最近积累的设置、存档、弹窗、CG和外框阴影；已通过且未变化的漫画、拳击、日志性能、复制/交换事务等路径沿用此前专项证据，不重复长测。使用真实游戏程序集及正式中文字体，在 CrossOver 隔离副本运行；没有启动正式玩家进程。

## 结果

- 五设置页等宽209.6、高64、间距32、总跨度1176；模组配置恢复独立页。草稿切页保留、取消不落盘、应用生效、缓存复用通过。25次缓存切页同步调用共49.04ms，约1.96ms/次；这是CPU调用计时，不含0.30秒设计动画。
- 四种存档配色、12槽4×3、鼠标大滚动量仍只切一整页、返回票券分段伸缩和文字右移8单位通过。已查看1920×1080和1280×720截图；大外框柔边阴影可见、无重描边，保持参考的轻微阴影观感，未声称不同背景下逐像素相同。
- 用实际游戏生成手动/自动档验证非空列表筛选，自动开关与删除模式可同时启用；手动/自动不混入快速档。默认玩家名称、地点、季节文字着色和手写备注优先通过。新增“学校教学楼”原版档实际加载对应背景，未强制家背景；男女主角Q版在实际列表可见。四季说明格式检查不等于四季完整剧情覆盖。
- CG实际保存到文件、列表CG预览、完整恢复、转回普通对白通过；追加小窗/全屏CG逐字途中暂停捕获、恢复后完成原句、连续切换CG、姓名普通字重通过。不同玩家性别环境及多URL配置有取样，未遍历所有性别CG组合。
- 确认/备注打开关闭为全屏水平百叶；已查看动画中间帧。实际备注光标网格可见，中文多行、选区高亮、保存后读回内容一致。没有将程序设置文字等同于真实中文输入法组合输入测试。
- 普通/CG工具栏黑色悬停文字移除；存档按钮悬停及点击音效调用通过（音效计数，不等同于人工听感验收）。ADV首开原生预载转圈标志关闭通过；真正存读档流程仍运行。
- 连续确认排队、退出动画完成后执行一次、取消一次、卸载取消尚未执行的确认、卸载恢复原生窗口/字体及撤销自有补丁通过。

## 本轮修复

回溯确认的退出动画回调缺少与普通确认同等的卸载保护。现在控制器已释放时不再执行延迟回溯，并在回溯入口再次检查释放状态。定点测试在真实回溯确认框的退出动画中销毁模组宿主，确认回调不执行且画布/输入遮罩完全释放。修改位置：`src/UI/AdvDialogueController.cs`。

测试脚本同步增加新五页布局、真实自动档、原生MiniCG和退出边界。早期中断是测试等待条件过早（界面尚未完成第一帧/重建）、逐字捕获未先取得菜单暂停、TMP测试属性/按钮名不匹配；均保留记录，未通过放宽生产门禁解决。

## CG“悬浮”截图说明

用户17:40截图来自本轮专门触发的原版MiniCG测试。反编译的`NewTalkView.ShowMiniCG`传入第三参数1，`CGView.Refresh`因此显示`icon_cg_mini`；全屏使用`icon_cg`。两种模式切换原版/ADV后的图像矩形一致，小窗保留场景背景，全屏铺满。没有把全屏CG统一缩小。

## 独立检查与证据

- 独立存储104项、严格配置兼容26项、历史编码16项、执行确认11项、游戏API元数据契约121项通过，生产编译通过。
- `qa/final-batch-20260924/stage-one/`：五设置页/四主题/分辨率/原生筛选/CG文件保存及备注光标。该轮在后续MiniCG测试等待条件处中断；只采用中断前已完成断言。
- `stage-two/`：修正测试捕获方式后，MiniCG/全屏逐字恢复、确认队列、完整卸载通过。
- `stage-three/`：非家背景、中文多行备注/选区、百叶中间帧、CG原版几何对照通过。
- `stage-four/`：最终生产修复后的回溯退出卸载回归通过；本轮仅修改此保护，未重复无变化的UI长测。
- 本机安装/备份/最终哈希：`qa/final-batch-20260924/install-receipt.json`。最终生产DLL与测试生产DLL一致；未安装QA重写程序集，未上传工坊/GitHub。
- 正式数据及原生偏好1349项基线在测试和安装后不变：`live-baseline-verification.json`。

## 尚不属于本轮通过范围

真实中文输入法组合输入、人工音效听感、全剧情/多模组长周目、所有显示缩放与离焦组合、Windows原生及双设备云同步。继续列在`PENDING_TESTS.md`，不把取样截图当作这些范围的验收。
