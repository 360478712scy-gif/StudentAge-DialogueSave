# DialogueSave 项目工作约定

## 当前接续（2026-09-25）

- 0.2.1已发布并逐文件下载校验：保存页空槽保存/非空覆盖，读取与快速读取空槽不写。112存储/26配置/16历史及隔离游戏点击覆盖/取消/无确认/恢复/原版覆盖通过。本机订阅包同步，见 docs/QA_SAVE_ROUTING_20260925.md 与 qa/releases/0.2.1/release-state.json。

- 0.2.0按用户授权发布：插件/图标/介绍提交成功；宣传图仅第1张已换，第2张Steam超时，余图尚待提交；真实下载校验未完成。进度qa/releases/0.2.0/release-state.json，不得称完整发布。用户另要五图压到1.9MB，已交付桌面高质量JPEG。

- 最新小修：标题无保存入口，标题加载页保存标签灰色不可点/空槽不写；同一存档分类重复点击不转场也不重置状态。仅编译、未跑整套测试（用户要求快改），安装qa/title-tabs-20260924/install-receipt.json，定点待测见PENDING_TESTS。首次选择按用户要求临时设Ask，待用户检查选择。

- 最终合批已过，报告 docs/QA_FINAL_20260924.md；104存储/26配置/16历史/11确认/121契约、五设置页、原版非家/自动筛选、CG全屏及MiniCG逐字恢复、弹窗/卸载通过。修复回溯关闭延迟回调卸载保护。真实输入法/听感/长周目/双设备仍待测，不把取样当全剧情。
- 存档12槽4×3、整页滚轮、四主题柔边阴影和440票券文字右移8已看1920/1280截图。正常全屏CG未缩小，用户17:40悬浮图是原生MiniCG专项测试，原版/ADV几何对照通过。
- 本机最新生产安装/备份/哈希：qa/final-batch-20260924/install-receipt.json；正式1349项基线不变。不可杀正式游戏，不安装QA程序集，不上传工坊/GitHub。
- 大量未提交改动包含前几批成果，不得reset/clean。当前大测试已经修正原版设置草稿刷新、漫画图文门控、存档图片/图集路径与回调重入、原版编辑映射盘检查。
- 用户要求精简上下文：先本节→待测清单顶部→数据库命中主题；不要全扫旧STATUS/qa/聊天。新证据更新已有条目，历史失败截图在本批目录。
- 漫画保留原生字幕、等图片就绪后显示，隐藏普通对话框与操作栏；hotfix-pass连续页和回溯/兼容/退出保存已通过。测试版会临时隔离手动输入，不是可玩预览；遇到卡住先看具体断言与日志。
- 当前五设置标签：游戏/图像/音频/模组配置/对话框，1176总跨度内等高等宽等间距；Original设置的对话界面行在快进速度下面、确认提交/取消撤销。首启校园蓝双预览/ADV推荐保留。

- 本批八项：弹窗开关整屏水平百叶、移除工具栏黑字、备注光标、原版默认说明/地点背景、五设置标签、CG快照预览与恢复、独立原版自动档筛选、ADV首开存档去转圈。当前验证/安装见 docs/QA_MODAL_EIGHT_20260924.md；12槽整页规则不变。

## 先定位，再读取

1. 本仓库是源码权威。先读本文件，再用 `python3 tools/knowledge.py "关键词"` 查询；无参数仅列主题，`--id settings.footer` 精确读取。
2. 只读命中条目的 files、相关函数和检查入口，不默认扫描全部 qa/、素材库、聊天或历史 STATUS。
3. 数据库是导航与已确认决策，不替代源码证据。安装/加载/版本/发布须核对当前文件、日志和收据；`docs/STATUS.md` 从顶部按需读。
4. 用户当轮要求优先；本文件保留核心约束，`docs/knowledge/project.json` 保存当前细节。历史中已被覆盖的要求不再生效。

## 授权与工作方式

- 默认主 agent 独立完成；仅在用户要求或当前适用规则允许时作一次限定审查，不持续启动后台审查。
- 正式游戏、真实存档、其他模组默认只读。会话已明确授权的本机安装可继续，不重复询问；本机替换不等于上传 Steam/GitHub。历史工坊授权不自动扩大为每轮发布。
- 测试数据只写 tests/、qa/；启动隔离玩家前必须由 prepare_qa.py 证明路径及原生身份隔离，不使用真实玩家存档作 fixture。
- 按用户当前批次要求分级验证；低风险待测项集中记 docs/PENDING_TESTS.md，不每轮开游戏。高风险做最小必要检查，最终合批保留存储/恢复门禁。文档、索引和整理不启动游戏。
- 汇报区分实现、独立检查、隔离游戏、正式游戏、发布和两设备云同步；编译、截图或 READY 日志不等于全剧情验收。

## 原生行为与数据底线

- 原版/ADV 在同一插件内，首选页两张预览整卡可选并持久保存。ADV 覆盖所有普通对白含闲聊；漫画保留原生，原生黑幕/背景过场首帧前隐藏插件 UI。切回原版完整恢复布局、字体及资源。
- 原版 Esc 只加保存/加载，保留原布局。Original模式原生手动/自动/快速分类、九宫格保留；对话存档是独立一级入口，其下才是自己的三类。原版键帽、克隆和入口细节查 ui.original。
- 选择、补全文字、跳过、回溯保留原生状态机/回调。禁用选项灰色不可点击，但仍能悬停查看条件；不重判资格，不篡改作者富文本。
- 操作不穿透弹窗；恢复隐藏不能顺便推进。空格隐藏/恢复、上滚开日志；选择时停止快进，跳过走原生执行至下一分支/小游戏/结束，不代替玩家选择。
- 保存请求覆盖播放、快进、普通/限时选项等状态，通过冻结、排队、去重和真实快照完成；排队成功不等于保存成功，不因适配不足静默禁用。
- 保存成功留在页面刷新；仅悬停/选中槽高亮，无额外侧边气泡。目标读档预检通过后放弃当前流程，不要求当前状态可保存，不先做当前状态的回滚备份。
- 检查点保留完整世界、效果阶段、随机状态、实际选项、continuation；回溯恢复效果状态，不靠改文本或重跑历史效果模拟。
- 有完整状态才允许跳转；不为旧文字日志伪造状态，不按任意条数/容量丢弃仍显示的历史。逐句历史只在内存；新档携带已有完整历史，固定分支后后台压缩，恢复前预检。
- 必须校验完整性、格式、必要节点/世界类型/continuation；不因插件/模组版本、DLL、游戏 MVID、全局摘要或启用列表不同直接拒读。实际缺失必须报错。
- .dsav 独立 dialogue_ 前缀，不改普通档 LatestSaveKey；同卷原子提交、独立回执、跨进程父版本检查，不按时间覆盖冲突。后台只处理主线程捕获的独立数据；单事务/代次令牌，读档取消旧捕获但完成已进入写入的事务。
- 每次保存和旧档扫描维护本地 Retained 副本，后台只补缺失、不覆盖、不复活删除标记；卸载不删档。退出自动保存有界等待、不拦退出不弹窗，不保证强杀/断电成功。

## 已定视觉原则

- 精确响应边框、状态、间距和作用区域，不顺手重画其他控件。当前校园蓝风格；旧暖白黑字、顶部模糊、顶部艺术字要求已被覆盖。
- 所有底部纸飞机票券按钮一律粗重、略倾斜艺术字，包括以后新增按钮；缺字稿要补素材，不能退回普通字体。顶部标签和中间选项用普通中文字体。
- 顶部保留原作圆角、下沿略厚和选中小箭头；黑字后白笔刷上下窄、两侧略长，仅 idle 显示，不模糊背景。
- 设置提示条、条件提示框、确认弹窗是不同组件，不混用；分别查 settings.footer、ui.conditions、ui.confirmations。
- 姓名统一普通字重【姓名】；普通对白加「」，CG 正文不加，CG 无水印/姓名框。CG姓名居中，灰白高光深边投影分隔线贴近上下文字；黑色原生底板按一/二/三行增高，保留透明渐变。阴影/括号只改呈现，不改原文/历史/作者富文本；验证实际中文字体，不把 QA 苹方当正式字体。
- 其余几何、状态、素材及证据只在知识库维护，不再持续追加相互覆盖的规则。

## 性能、生命周期与验收

- 禁止逐帧全场景查找、读盘、全量配置摘要、读插件 DLL、反复重建 UI 或强制 TMP 网格。字体/素材一次加载、缓存、释放。
- 配置摘要不进入正常保存/自动保存/退出保存或预热；独立诊断/捕获一致性如使用摘要须真实完整比较，不以 TTL/对象引用伪造命中。
- 相同对白 Anim/AnimEnd/Countdown 不重复捕获，选项独立记录；历史肖像按可见轮廓居中并缓存，日志只实例化可见行并保留至多24条屏外渲染缓存，不缓存或裁掉历史数据。
- 独立 GUID/Harmony ID 和 DontDestroyOnLoad 宿主；BepInEx_Manager 销毁不是卸载。仅撤销自己的补丁，适配/存储/UI 分离，反射集中且失败保留原版可用，不改 UP/其他插件。
- 构建→隔离证明→针对性 QA→目视截图→有安装授权时备份、替换生产 DLL、核对哈希；绝不安装 QA 重写程序集，不恢复已删除猫咪美化资产。
- 画面检查包括原有和新增控件；功能门禁覆盖原子提交/损坏/冲突、效果恰好一次、分支恢复、生命周期/组合兼容；云同步单列两设备验收。
- 按 docs/knowledge/README.md 更新已有条目：最多 24 个主题，每条最多 5 个要点，保留来源及未验证边界，不录入全文日志。
