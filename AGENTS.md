# 学生时代 · 对话存档项目总指导

## 用户授权与边界
- 当前任务已授权实现独立 BepInEx 对话存档插件、隔离验证、可回滚测试包，以及多子 agent 审查。
- 正式游戏、其他项目、用户 Mod 与真实存档只读。没有安装授权，不自动安装进正式游戏。用户已授权本项目首次上传 GitHub；后续发布、推送以当前任务授权为准。
- 所有测试只写本项目 tests/ 或 qa/ 下隔离数据；运行游戏测试必须先证明存档路径已重定向。
- 不恢复或复用已删除的猫咪美化资产；原版模式复用游戏 UI，ADV 模式由插件提供独立呈现。

## 2026-09-20 新增 ADV 模式（最新要求优先）
- 新对话 UI 属于本存档插件，不另起独立插件；初次加载选择「原版／ADV 风」，持久记住选择，原生设置页可随时切换。
- 界面选择页左右各一张实际界面预览图；整张卡片可选。ADV 说明为“支持插件拓展功能（跳过,对话跳转等）”，原版说明为“不支持其他拓展功能，仅支持对话中保存”。
- 原版模式遵守下方原尺寸/原位置约束；ADV 模式允许替换对话框、选项、操作栏和日志布局，采用学生时代的校园纸感与暖色，参考本机 AfterSchool3D 的 Galgame 操作，不照搬其人物资产。
- ADV 包含保存/读取/快存/快读、自动、已读快进、隐藏界面、操作栏收起、设置、回看。隐藏后恢复的点击不能同时推进对白；选择时停止快进；所有动作不得穿透弹窗。
- 回看跳转必须恢复真实世界/选择/对白状态，不能只改文本或重跑奖励。未建立完整检查点的旧历史不能伪装可回退。
- 不给逐帧工作引入全场景查找、磁盘 I/O、全量 JSON 或反复重建 UI。实机比较帧耗时与打开面板耗时；未测得不得承诺性能等同原版。
- 用户要求节约 token，实现和验证阶段由主 agent 独立处理。完成后才允许一次美观与易用性子 agent 审查，结束后及时停止；不启动后端审查，覆盖下方旧的强制多 agent 规则。
- ADV 最新视觉：普通对白维持当前高度、很淡的暖白渐隐底、黑字；CG 更矮、黑底、较大居中浅色文字和居中姓名。选项与其他面板采用同一暖白色系。姓名牌随真实性别变色、随姓名长度变化，旁白不显示姓名牌；句尾黑边纸飞机轻微浮动。
- 底部保存四项为 SAVE / LOAD / Q.SAVE / Q.LOAD；自动为无横杠三角，独立剧情跳过为三角加竖杠，先确认并可勾选不再提示。顺序为 SAVE、LOAD、Q.SAVE、Q.LOAD、日志、自动、快进、跳过、隐藏、设置；整体靠右；收起/展开维持原来的左右箭头，中心固定，不可擅自换成上下图标。确认框收窄、不隐藏后方对白，无额外说明，两按钮分立左右。跳过直接抵达下一个选项/小游戏/结尾，复用原生剧情执行，不代替玩家选择、不逐句快进。
- 日志全屏、左侧原生历史服饰预览，有可用静态表情差分时显示差分；跳转按钮在对应台词下方，使用无文字的回转箭头按钮；条目分隔线需清晰可见。底部只有“返回对话”，无收藏/播放/音量/标题/流程图或翻页按钮；滚动条两端有到顶/到底按钮，日志底板半透明、显示背后对话，禁止额外标语；滚轮连续滚动，Esc 关闭。只有完整检查点可跳转，不能伪造旧历史的恢复能力。
- 最新要求：对白和选项都应能跳转；不得按 96 条/24MB 等配额丢弃仍显示在日志中的恢复数据，也不能在快进时停止记录。对白和选项分别保留恢复位置。跳转确认与跳过使用同款框，可独立记住不再提示，取消留在日志；底部不放状态、进度或错误文字，诊断只写后台。日志底色在前版上稍暗、稍透。旧版已经丢失的完整状态不能靠文字伪造；尚未适配的真实状态必须如实报告，不能宣称所有记录已经可恢复。
- 空格隐藏/恢复对白，不可同时推进或提交按钮；向上滚轮打开日志。ADV 接管必须发生在首帧绘制之前，不等原生 isViewReady；切回原版完整恢复原布局与字体。

## 本轮修复约束（2026-09-20）
- 日志回转箭头使用平滑粗细一致的弧线与抗锯齿，禁止退回细折线；底色为 230/228/222、alpha 0.86。
- 相同对白的 Anim/AnimEnd/Countdown 不重复生成世界快照；选项仍必须有独立记录。配置缓存复用仍完整逐字段比较，不能用跳过校验换性能。
- 历史人物预览按可见轮廓横向居中，避免透明画布留白导致位置漂移；采样仅在首次遇到该资源/裁切方式时执行并缓存，恢复过程不显示“正在恢复对话”或其他状态标签。
- 删除与列表扫描相遇时保留这次请求，待扫描结束完成，不能让玩家再次点击；重复请求合并且只删除指定版本。
- 新对话档内嵌已有完整历史检查点的压缩数据；捕获时固定历史分支，后台只压缩独立 DTO。读取前检查所属周目和日志前缀，实际跳转仍完整预检。旧档无此可选字段时保留原兼容性，不能凭文字伪造旧状态。

## 产品要求
- 用户最新纠正：对话右下角操作栏提供保存、加载、快速保存、快速加载四项，白色按键框只放这里。对话 Esc 菜单只增加“保存游戏”“加载游戏”两项，完全复用原菜单按钮；禁止新增快速保存/快速加载菜单项，禁止菜单中显示快捷键图标。默认快捷键 S/L/H/J，先检测对话上下文原生冲突，有冲突才更换并显示实际键位；不影响对话外原生按键，也不重复触发已有快存快读。
- 用户已澄清：原读档窗口手动/自动/快速三个按钮完全保留，不增加“普通存档”父级。顶部始终是“对话存档｜手动存档｜自动存档｜快速存档”；“对话存档”不得改名成“返回存档”。选中它后，下方另显示对话档自己的手动/自动/快速二级分类，不能占用或隐藏原三个一级按钮。
- 保存界面只在原手动存档左侧新增对话存档。卡片直接显示说话人与台词摘要，保留九宫格。
- 最新要求：选项页（含限时选择）属于对话存档；任何对话状态都可发起保存，不因播放/快进/动画/来源不在首批适配范围而灰掉按钮或要求用户重按。内部负责冻结与一致性快照、必要的异步等待和请求去重。不能把仅登记请求或缺少恢复上下文的文件当作保存成功。
- 未加载插件不出现新增入口，卸载不删除对话档；绝不修改普通存档 LatestSaveKey。
- 原尺寸、字体、边框、按钮状态、声音、Canvas 缩放、输入行为沿用原版；必须真实截图审查，编译/日志不能代替视觉验收。工具栏检查必须覆盖原版全部按钮与新增按钮，不得只检查新增四项就宣称不重叠。文字字形、键帽长宽比、字母居中、重复渲染和菜单额外图标均须目视逐项确认。
- 键帽英文参照原版common6单键F/S：棕色#734D31、偏粗无衬线字形及原字号字面高度；不得使用黑色细体。中文标题以当前可见日志按钮为克隆模板，保留完整字体与材质，不从隐藏池单元取样。
- 位置必须沿用原版：不得将 Esc 菜单重设为屏幕居中，不修改原生按钮组锚点、位置、缩放和布局参数。新增保存/载入沿用原生五项菜单布局；右下新增四项向左扩展，日志/自动/菜单保持原位置。截图须与未装饰的原生界面对照。

- 最新反馈：保存成功留在保存页刷新卡片；只有鼠标当前悬停或选中槽高亮，不得因事务busy把所有空槽套用原版黄边禁用样式。保存、读取（含快速操作）不产生右侧气泡；真实错误在本页底部或原生错误框说明。
- 读档代表放弃当前进度：目标存档预检通过后直接废弃旧流程，不要求当前状态可保存，不捕获当前状态作为读档前置或回滚；原存档完整性与兼容校验保留。

## 存储与一致性
- 完整、自包含、版本化 .dsav 文件平铺原版用户存档目录，可匹配当前 Steam AutoCloud 通配规则。使用 dialogue_ 独立前缀，不伪装普通 .save。
- 临时文件与备份在同卷、非云目录，校验后原子发布；保存成功只表示本地落盘，不表示云同步完成。
- 槽位与 revision 分离；独立唯一 revision，parentRevisionId 可识别分叉。不以文件时间静默解决跨设备冲突。
- 自身插件以精确 version 与 format/adapter 作为兼容契约；仅 UI/性能重编译不因自身 DLL hash 不同拒读旧档，第三方插件二进制仍严格匹配。恢复语义/DTO 有不兼容变化必须升级相应版本。
- UI 能解释不兼容/损坏，不反序列化任意类型，不覆盖不认识的高版本格式。
- 主线程捕获独立快照，后台只处理已脱离活动对象的字节/DTO。单事务、代次令牌、取消旧回调。保存/读取繁忙时排队；读档取消尚未提交的旧世界捕获，已进入落盘的写入必须完成。
- 保存成功以独立提交回执为准，不依赖界面调度队列存活；回调异常不能改变提交结果或堵住后续操作。存储跨进程比较父版本并原子发布；默认不设置历史总容量/数量配额。
- 配置摘要仅在完整比较实际字段、集合内容与顺序一致后复用；不能以TTL、对象引用或集合数量代替比较。隔离QA测试驱动的重编译兼容只能在已证明隔离的测试程序集内处理，不能混入生产依赖白名单。
- 恢复必须包含世界、对话段落、效果阶段、实际选项和继续上下文。不能只存 talkId 或直接重跑 RefreshTalk/普通 LoadGame。
- 稳定节点可以直接捕获，其他状态须由捕获协调器处理暂停/过渡/恢复上下文。不得用默认 callback=null 假设安全；尚未完成的上下文适配必须如实记录为未实现，继续补齐，而非在界面上禁用后称需求已完成。
- 恢复/清理不结算当前剧情，不重复奖励、回合推进或全局写入。

## 架构与生命周期
- 独立插件 GUID 与 Harmony ID；独立 HideAndDontSave + DontDestroyOnLoad 宿主。BepInEx_Manager 被游戏销毁不是卸载信号。
- GameAdapter、Capture/Restore、Repository、UI 分离；版本敏感反射集中，自检失败关闭对应功能，保留原版可用。
- 不更改 UP 或其他插件代码/补丁；与音频/小游戏兼容通过可选适配与明确支持范围处理。
- 自己的补丁只撤销自己的 ID，避免破坏原有插件。

## 协作与审查
- 默认主 agent 独立完成；子 agent 仅按上方最新授权进行一次限定的美观与易用性审查，不启动持续后台审查。
- 禁止把隔离测试、合成 fixture、只读反编译或 READY 日志写成完整游戏/跨电脑云存档已验收。
- 当前状态写 docs/STATUS.md；保留未通过门禁和证据，不用完成措辞掩盖缺项。

## 验证效率
- 一轮修改集中完成后统一验证，禁止每个小修改都重启游戏。纯 UI 微调做定点画面检查，存储/恢复逻辑修改跑对应功能回归；仅实际失败需要复测时才重启，并说明原因。
- 仓库整理、文档和 CI 修改不启动游戏。构建与独立测试可批量验证。

## 验收门禁
1. 仓库测试：原子落盘、中断、损坏、版本、冲突、路径隔离。
2. 恢复语义：已执行效果不重放；未执行效果继续恰好一次；段落/随机选项/后续事件一致。
3. 原生 UI：四按钮和层级、九宫格摘要、按键/点击无穿透、不同分辨率、多次开关无重复。
4. 生命周期/兼容：引导对象销毁后仍生效；UP/音频组合；卸载原版可用且档案保留。
5. 云端：两设备真同步单列验收；没有第二设备或真实上传证据就标未验证。

## 2026-09-20 模组更新兼容与 Steam 测试包（最新要求）
- 不因模组版本、插件版本/二进制、游戏 MVID、全局配置摘要或启用列表不同拒读。以上旧严格匹配要求被本条覆盖；校验存档完整性、格式、真实需要的节点、世界类型与 continuation。
- 配置摘要保留作诊断及捕获快照一致性用途，不是读档版本门槛。真正缺失的关键节点仍须明确错误，禁止伪造恢复。
- 对话中返回标题/正常退出自动保存独立自动档；失败或有限等待超时不拦截退出、不弹提示。不能承诺强杀/断电/不可写磁盘必定成功。
- 按游戏原生 plugins/ 目录和小游戏项目同款清单打包；本轮只制作 Steam 模组包，不发布工坊，不推送代码或发布 GitHub Release。

## 最新追加：回溯开销与漫画
- 日志回跳恢复独立世界快照与效果阶段，已执行的选项/对白效果一起回溯；禁止仅改变文本或重跑历史效果来模拟回溯。逐句记录只在内存，不产生逐句磁盘存档；不新增提示。
- 历史位置无需保存/遍历全量配置或读取插件 DLL；全局摘要已不是恢复门槛。完整世界数据、随机状态、实际选项和 continuation 仍保留，不能用漏记效果换取性能。
- 漫画演出（ComicView）采用原版文字、字体、位置，不显示 ADV 对话底板、工具栏或额外原版样式四按钮。此例外不影响普通对白及真正 CG 的现有样式。

## Steam 发布最新授权
- 用户已明确授权发布新工坊条目，名称“对话中存档（仅测试版支持）”，使用指定封面文案、四张截图、宣传文，允许留言，并验证真实订阅启用。此授权覆盖此前仅打包不发布限制。
- 订阅验证沿用隔离数据目录，允许所需 Steam UGC 创建、更新、订阅、下载；其他模组与正式存档不改动。

## 最新追加：事件 UI 与浮现性能（2026-09-21）
- ADV 只用于真正绑定事件的对白；所有无事件对白保留原版 UI，不显示插件原版保存四按钮。按 ShowEvent/ShowTalk 的真实调用来源记录，不以复用视图的 evtId、台词关键词或时间绑定判断。
- 原生 WaitBg/BlackBg 及沿用这些演出的剧情 Mod 过场期间，在首帧前隐藏插件呈现与新增控制；过场结束恢复，CG 与漫画现有规则不变。
- 点击补全保持原生状态机和效果执行；避免后台原版工具栏反复重建、每帧重复呈现同步及重复强制 TMP 网格更新。性能结果须区分同步点击耗时与完整帧耗时。

## 最新追加：保存后台开销（2026-09-21）
- 全局配置指纹与插件 DLL 哈希不再进入任何正常保存/自动保存/退出保存或逐帧预热路径；保留空的旧诊断字段以兼容旧格式，不能伪造摘要缓存。独立诊断工具如计算摘要仍须完整比较。
- 不减少世界快照、效果阶段、随机状态、选项回调与 continuation，不移除文件完整性和原子提交校验。性能回归覆盖真实自动存档流程，不能只测点击回调。

- 最新用户要求：除原生本地云同步目录外，每次保存还在 DialogueSaveLocal/<SteamId>/Backups/Retained 持久保留独立副本。同步丢失后后台恢复缺失文件；不覆盖已有文件，不复活有删除标记的记录。已有档扫描补备份，后台执行、不新增弹窗。不将整个历史版本列表误报为当前可见槽位数量。

## 最新 UI 规则（覆盖事件限定）
- 用户取消非事件对白排除：选择 ADV 后所有普通对白统一使用 ADV，闲聊也包括在内；原版模式仍可整体选择。漫画与原生过场继续保持之前的无插件界面规则。
